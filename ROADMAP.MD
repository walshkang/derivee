# Dérivée — Unified Roadmap

This document is the single source of truth for all planned development waves.

> [!IMPORTANT]
> **Agent Roadmap Maintenance Protocol**:
> 1. When completing a task, update its status in the **Master Status Table** from `Planned` to `✅ Done`.
> 2. Move the completed task's detailed prompt out of this file and append it to `archive/shipped-waves-archive.md`.
> 3. Keep this file lean to minimize context bloat.

> [!WARNING]
> **Architectural Transition in Progress.** All pre-Wave-A development (Waves 1–14.10) was completed under the original Expo Managed / JS-background architecture. Those waves are archived in `archive/shipped-waves-archive.md`. Waves that relied on JavaScript background execution (W4-TRACK, W14.5-BACKGROUND, W14.6, W14.7, W14.8) are marked as **🔄 Superseded** — their functionality is replaced by the Sleepy Hermes Transition (Wave A–D). Waves E–H align the UI to the updated `docs/design.md` specification and prepare for App Store submission.

---

## Master Status Table

### Completed Waves (Pre-Sleepy Hermes — Archived)

| Wave | Task ID | Title | Status |
|:---:|:---:|:---|:---:|
| 1 | W1-INIT | Expo Scaffold & Zustand State | ✅ Archived |
| 1 | W1-NAV | Navigation Shell & Splash Screen | ✅ Archived |
| 2 | W2-DB | op-sqlite JSI Initialization & WAL Mode | ✅ Archived |
| 2 | W2-H3 | h3-js Core Logic & Grid Conversions | ✅ Archived |
| 3 | W3-MAP | MapLibre Engine & 3D Terrain Base | ✅ Archived |
| 3 | W3-FOG | 50km Bounding Box & Inverted Polygon | ✅ Archived |
| 4 | W4-TRACK | expo-location Background Service & Batching | 🔄 Superseded → Wave C |
| 4 | W4-DRIFT | Implied Speed Filter & Geometry Unioning | 🔄 Superseded → Wave C |
| 5 | W5-POI | Gamification & POI Integration | ✅ Archived |
| 6 | W6-POLISH | Performance & App Store Launch | ✅ Archived |
| 7 | W7-AMBIENT | Vicinity Bubble & Ghost POI Logic | ✅ Archived |
| 8 | W8-TRANSIT | Live GTFS-RT Decoding & Transit Bottom-Sheet | ✅ Archived |
| 8.1 | W8.1-TRANSIT-REFACTOR | Dual-Format Transit Parser (JSON + PB) & API Keys | ✅ Archived |
| 9 | W9-OBSERVER | Headless Observer Tool & CDN Handoff | ✅ Archived |
| 10 | W10-HISTORIAN | GPX/HealthKit Import & Macro-Reveal Animation | ✅ Archived |
| 11 | W11-TIMETABLE | Standalone Transit App (Web/Spinoff) & Headway Matrix | ✅ Archived |
| 11.5 | W11.5-MVP-POLISH | Light Mode UI Refactor & MVP Launch | ✅ Archived |
| 13 | W13-FOG-TIERS | 3-Tier Fog State & Reference Counting | ✅ Archived |
| 13 | W13-FOG-STYLE | MapLibre Dual-Layer & Temporal Interpolation | ✅ Archived |
| 13 | W13-AMBIENT-TRACKING | Ambient Tracking & Start Button Removal | ✅ Archived |
| 14 | W14-DATA-NEIGHBORHOODS | Neighborhood Denominators & Water Subtraction | ✅ Archived |
| 14 | W14-UI-STATS | Progression Stats UI: Contextual Pill & Stats and Profile Screen | ✅ Archived |
| 14.5 | W14.5-BACKGROUND | Local Expo Module for iOS Background Assertions | 🔄 Superseded → Wave C |
| 14.5 | W14.5-DB-MIGRATE | SQLite geojson_cache & Splash Screen Gate | ✅ Archived |
| 14.5 | W14.5-STORE | AppState Delta-Buffer & Zustand Refactor | 🔄 Superseded → Wave D |
| 14.5 | W14.5-RENDER | MapLibre Native Array Concatenation (Remove Turf) | ✅ Archived |
| 14.6 | W14.6-BATTERY-HOTFIX | Bridge & Polling Optimization Patch | 🔄 Superseded → Wave A/D |
| 14.7 | W14.7-LOCATION-AUDIT | Stop the Bleeding: expo-location Audit | 🔄 Superseded → Wave C |
| 14.8 | W14.8-SET-GATE | Unblock the Thread: In-Memory Set Gate | 🔄 Superseded → Wave D |
| 14.9 | W14.9-DDS-FOG | DDS Refactor: MapLibre Data-Driven Styling | ✅ Archived |
| 14.10 | W14.10-STATS-UI | Fix UI Bottlenecks: FlashList & Bottom Sheet | ✅ Archived |

### 🚀 Pure Native iOS Migration (Complete)

The project has fully abandoned the hybrid React Native / Expo architecture. All tracking, database, and UI logic is now executed natively in Swift within the `DeriveeNative/` folder.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **N.1** | **WN1-XCODEGEN** | **Pure Native Project Setup (xcodegen + H3)** | **M** | ✅ Done |
| **N.2** | **WN2-GRDB** | **GRDB SQLite Manager & Observable Store** | **M** | ✅ Done |
| **N.3** | **WN3-TRACKING** | **Ambient Tracking Engine (liveUpdates + Shield)** | **L** | ✅ Done |

### 🧪 Testing & CI/CD Rollout

| Phase | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **1** | **TEST-PHASE1** | **Core Engine Unit Tests (Background & SQLite)** | **M** | ✅ Done |
| **2** | **TEST-PHASE2** | **Spatial Math & Transit Hydration Unit Tests** | **S** | ✅ Done |
| **3** | **TEST-PHASE3** | **Automated UI / Snapshot Testing** | **M** | ✅ Done |
| **4** | **TEST-PHASE4** | **Local Shift-Left Ergonomics & Test Bouncer** | **M** | ✅ Done (Supersedes Remote CI) |

### 🎨 Design Alignment & Ship

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **11.6** | **W11.6-DEVOPS** | **Deploy Go Observer to Oracle Cloud & Seed R2** | **M** | ✅ Done |
| **11.8** | **W11.8-WEB-FIXES** | **Transit Web MVP Bugfixes & Polish** | **M** | ✅ Done |
| **E** | **WE-ONBOARDING** | **Onboarding Gate & First-Launch Infrastructure** | **M** | ✅ Done |
| **F.1** | **WF1-FAB** | **Map UI: Floating Action Buttons** | **S** | ✅ Done |
| **F.2** | **WF2-TOAST** | **Discovery Toast Overlay** | **S** | ✅ Done |
| **F.3** | **WF3-POI-LIFECYCLE** | **Ghost POI 3-Phase Lifecycle** | **M** | ✅ Done |
| **F.4** | **WF4-HEX-ANIMATION** | **Ambient Hex Unlock Animation** | **S** | ✅ Done |
| **G** | **WG-TRANSIT-REVEAL** | **Transit Reveal Enhancements** | **S** | ✅ Done |
| **H** | **WH-SHIP** | **Polish, Stats and Profile Rewire & Ship Prep** | **M** | ✅ Done |
| **H.1** | **WH.1-PANNING** | **Stats UI: Neighborhood Map Panning** | **S** | ✅ Done |

### 🐛 Fog Reliability & Startup Performance (Cold-Start Fix)

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **I.1** | **WI1-FOG-GATE** | **Fog Startup Gate: Synchronize Shape Computation with Map Ready** | **M** | ✅ Done |
| **I.2** | **WI2-WINDING** | **Winding Order Audit & Interior Ring Hardening** | **S** | ✅ Done |
| **I.3** | **WI3-FOG-TESTS** | **Cold-Start Fog Regression Tests** | **S** | ✅ Done |
| **I.3a** | **WI3a-LIVE-FOG** | **Fix Live Fog Update Starvation (`.background` → `.userInitiated`)** | **XS** | ✅ Done |
| **I.4** | **WI4-ASYNC-READS** | **Eliminate Main-Thread DB Reads & Configure GRDB QoS** | **M** | ✅ Done |
| **I.5** | **WI5-HANG-TESTS** | **Priority Inversion Regression Tests** | **S** | ✅ Done |
| **I.6** | **WI6-TRACK-STOP** | **Fix Ambient Tracking Stop Lifecycle (Live Activity + Persistence)** | **S** | ✅ Done |
| **I.7** | **WI7-LA-CLEANUP** | **Clean up Orphaned Live Activities (Dynamic Island) on App Force-Quit / Cold Start** | **S** | ✅ Done |
| **I.8** | **WI8-OBS-DIAG** | **GRDB ValueObservation Diagnostic Instrumentation: Add `deinit` log to `SpatialStore`, pool `ObjectIdentifier` logs to `SpatialStore.startObservation` + `AmbientTrackingEngine.processLocation` + `SpatialDatabaseManager.init`. Logging only — zero logic changes. Build on physical iPhone, walk until hex insert, read logs to confirm observation alive + pool identity match.** | **XS** | ✅ Done |
| **I.9** | **WI9-OBS-FIX** | **GRDB ValueObservation Pipeline Hardening: Diagnostic testing revealed a silent `ValueObservation` failure in GRDB. An isolated minimal test confirmed that even when `insertDiscoveredHex` successfully writes to the `DatabasePool` (changesCount > 0), the `onChange` closure never fires for the update. Root cause hypotheses: (1) `ATTACH DATABASE` confusing region tracking, (2) Raw SQL `String.fetchAll` missing updates compared to `TableRecord`, (3) `.withoutRowID` table tracking limitations. Next Steps: Fix the observation query (e.g., use standard `TableRecord` or verify `.observing(DatabaseRegion(table: "explored_hexes"))` syntax for GRDB 6.29.3) so the minimal test passes, then apply to `SpatialStore` and fix `generateH3Hexes` non-determinism.** | **S** | ✅ Done |
| **I.10a** | **WI10a-PIPELINE-LOGS** | **Add 6-stage diagnostic logging to the live hex unlock pipeline via `PipelineLogger` (stdout + `os.Logger` + `Documents/pipeline_debug.log`). Enabled `UIFileSharingEnabled` & `LSSupportsOpeningDocumentsInPlace` for iOS Files App access during untethered field testing. [S1] `resumeTrackingIfNeeded` entry + `isTrackingEnabled`. [S2] `processLocation` entry + hex string + timestamp + speed. [S3] `insertDiscoveredHex` result (`isNew`). [S4] `onChange` hex count + Thread. [S5] `recomputeFogShape` cancellation checkpoints. [S6] `updateExploredHexes` shape presence + interior polygon count.** | **XS** | ✅ Done |
| **I.10b** | **WI10b-ONDEVICE-WALK** | **🧑‍💻 Human-only task (no agent). Build I.10a on physical iPhone. Force-close the app. Reopen. Walk across 3+ hex boundaries untethered. Open iOS **Files** app → **On My iPhone** → **Dérivée** → AirDrop `pipeline_debug.log` (or copy console logs). Paste logs into a new conversation for I.10c to interpret.** | **XS** | ✅ Done |
| **I.10c** | **WI10c-LOG-INTERPRET** | **Read the on-device logs from I.10b (`pipeline_debug.log`) and identify which pipeline stage breaks. Decision tree: If S2 never fires → `CLLocationUpdate.liveUpdates()` warm-up delay. If S2 fires but S3 shows `isNew=false` → `INSERT OR IGNORE` dedup (user still on same hex). If S3 shows `isNew=true` but S4 doesn't fire → GRDB `ValueObservation` issue. If S4 fires but S5 shows `cancelled=true` → `recomputeFogShape` cancellation race. If S5 completes but S6 doesn't fire → SwiftUI `@Observable` → `UIViewRepresentable` bridge issue. File findings and the targeted fix as I.11.** | **XS** | ✅ Done |

### 🚀 Exploration, Performance & Map Customization (Wave J — Post-Debugging)

> [!NOTE]
> **Execution Cadence:** Wave J sub-waves execute in strict sequential order (J.2 → J.8). Each targets a distinct architectural layer; building and verifying independently prevents cascading style/shader regressions on the 120Hz render loop.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **J.1** | **WJ1-CAMERA-BOUNDS** | **Camera Clamping, 2D Top-Down Lock & Rubber-Band Damping:** Lock camera strictly to 2D top-down view (`pitch = 0`, `mapView.allowsTilting = false`). Reject `.gestureTilt` and `pitch > 0` in `shouldChangeFrom:to:reason:` delegate. Asynchronous `.easeOut` boundary rollback for rubber-band stretch. Enable full-zoom 2D building footprints (`z = 13..24`) and disable 3D fill-extrusions to ensure 1:1 geographic sync with Fog of War. | **S** | ✅ Done |
| **J.2** | **WJ2-PERF-OPTIMIZATION** | **120Hz Spatial Unioning & Render Optimization:** Full Res-11 dissolution via `cellsToLinkedMultiPolygon` in `Task.detached(priority: .userInitiated)`. Traverses `LinkedGeoPolygon` cluster outer boundary loops (`first`), reverses vertices for CW winding order, and injects as interior holes in bounding box `MLNPolygon` (>85% vertex reduction), targeting <1.5ms `@MainActor` shape assignment. | **M** | ✅ Done |
| **J.3** | **WJ3-FOG-POI-MASKING** | **Dual-Model Vector Tile POI Masking (Pure Separation):** Commercial/retail POIs in MapTiler base vector tiles suppressed via GPU `mgl_distanceFrom:(userPoint) <= 200` predicate. Interactive stations, ghost POIs, and permanent landmark pins rendered exclusively via native SQLite-backed `poiSourceId` runtime layer. | **S** | ✅ Done |
| **J.4** | **WJ4-BASEMAP-SWITCHER** | **Bundled Composite Style Basemap Switcher:** Local `composite_style.json` in app bundle with shared `maptiler_streets` vector source and pure Light Mode themes (Standard Exploration `#F9F9F6` and Transit Navigation `#FFFFFF`). Runtime key injection. Crossfade themes via `MLNTransition(duration: 0.6)` layer opacity/color transitions on GPU — zero style destruction, zero source unmounting. | **M** | ✅ Done |
| **J.5** | **WJ5-CUSTOMIZATION-SETTINGS** | **Zero-Geometry Customization Settings (Screen 3 + `@AppStorage`):** Fog opacity slider (`0.40`–`0.98`, auto-adapting 40% in Transit mode and 85% in Day mode) and hex/boundary borders via `MLNLineStyleLayer` attached directly to existing `fog-source` in `SettingsView`. `@AppStorage` persistence with `MLNTransition(duration: 0)` fragment shader updates — no polygon recomputation or DB queries. | **S** | ✅ Done |
| **J.6** | **WJ6-TRANSIT-NODE-TRACKING** | **Hybrid SwiftProtobuf Transit Sheet & Local Shape Fallback:** SPM `swift-protobuf` + pre-compiled `gtfs-realtime.pb.swift` models. Sheet-scoped 15s polling for live arrivals (cancelled on dismiss). Physical track/route geometries sourced from pre-hydrated local `derivee_transit.sqlite`. Dual-layer high-contrast route rendering (6px silver casing + 4px agency color). | **M** | ✅ Done |
| **J.7** | **WJ7-POI-GAMIFICATION** | **Dual-Sensory Discovery Loop & Categorized Milestone Journal:** Instant `UIImpactFeedbackGenerator(.light)` on GRDB insert + transient expanding `MLNCircleStyleLayer` pulse (0→80px, 1.2s fade) on the map. Screen 3 Exploration Journal with on-demand asynchronous GRDB aggregation (`dbWriter.read`) for categorized milestone progress cards (Transit Hubs, Neighborhoods, Landmarks). | **M** | ✅ Done |
| **J.8** | **WJ8-APERTURE-BRANDING** | **Unified Crystalline Aperture Rollout (AppIcon, FAB, Milestones & Live Activity):** (1) Generate Day/Night/iOS 18 Tinted `AppIcon.appiconset` vector assets. (2) Replace `person.crop.circle` in `ProfileFAB` with Variation A aperture micro-glyph in `.ultraThinMaterial`. (3) Upgrade Screen 0 Onboarding Gate with centered pulsing vector aperture during SQLite hydration. (4) Upgrade Screen 3 Exploration Journal with high-negative-space milestone cards and settings build signature. (5) Polish `TrackingLiveActivity` Dynamic Island compact leading micro-glyph and trailing distance metrics. (6) Style 120Hz native MapLibre compass needle in top-left matching brand identity. | **M** | ✅ Done |
| **J.9** | **WJ9-LIFECYCLE-HARDENING** | **Dynamic Island & Location Lifecycle Hardening on Force-Close:** Add `UIApplication.willTerminateNotification` observer in `AmbientTrackingEngine` to synchronously invalidate `CLBackgroundActivitySession` and end active `Activity<TrackingAttributes>`. Add rolling 2-minute `staleDate` fail-safe on `ActivityContent` and intra-hex distance heartbeat refresh. | **XS** | ✅ Done |
| **J.10** | **WJ10-DYNAMIC-ISLAND-CONTRACT** | **Decoupled Ambient Tracking & Dynamic Island Lifecycle Contract:** (1) Decouple Live Activity from mandatory background tracking with `@AppStorage("isLiveActivityEnabled")` sub-toggle under Settings > Tracking. (2) Shorten rolling `staleDate` to 45s so Dynamic Island auto-dismisses if app is killed. (3) Clean up all orphaned activities before requesting new ones. (4) Wire `derivee://progress` deep link via `.onOpenURL` to trigger map recenter and aperture pulse glow. (5) Comprehensive unit tests for toggle and lifecycle contracts. | **S** | ✅ Done |
| **J.11** | **WJ11-FOG-ISLANDS** | **Closed Loop Fog Islands via FogGeometry Composite Shapes:** Traverse interior `LinkedGeoLoop.next` holes within `LinkedGeoPolygon` clusters to extract unvisited center island polygons, reversing vertex coordinates for clockwise outer winding. Construct composite `MLNShapeCollection` containing the main bounding box polygon with interior cutout holes plus disjoint interior fog island polygons, accurately preserving unexplored city blocks encircled by closed-loop walks. | **S** | ✅ Done |
| **J.12** | **WJ12-LOCATION-FILTER** | **Multi-Stage Cold Start Location Filter & Contextual Drift Gate:** Implement `ColdStartLocationFilter` replacing raw speed gating with Rayleigh horizontal uncertainty bounding ($\le 25\text{m}$ for H3 Res 11), cache staleness check ($\le 5\text{s}$), instant unlock for high-accuracy fixes ($\le 12\text{m}$), 3.0-second stationary dwell fallback ($12\text{m} < \text{hAcc} \le 25\text{m}$), and subway emergence discontinuity classifier ($\Delta t \ge 15\text{s}$). Resets baseline velocity jumps without dropping subway arrival emergence fixes. | **M** | ✅ Done |
| **J.13** | **WJ13-THOROUGHFARES-BUS-LENS** | **MTA Subway Thoroughfares Under Fog & Nearby Bus Quick Lens:** (1) Pre-bundle full NYC subway line geometries (`MtaSubwayNetworkData`) as an ambient orienting sub-context beneath the fog of war with day/night adaptive casings. (2) Floating frosted-glass `NearbyBusesCapsule` quick lens with 400m spatial SQLite queries, real-time Protobuf bus arrival deserialization, and Settings toggle (`showNearbyBusesLens`). | **M** | ✅ Done |
| **J.14** | **WJ14-LOCATION-BRIDGE** | **MapLibre Location Bridge & Decoupled UI GPS Resolution:** (1) Bi-directional `currentUserLocation` binding from `MapView` to `ContentView` bridging live MapLibre GPS updates to the HUD. (2) Decouple immediate UI location updates in `AmbientTrackingEngine` from strict multi-fix cold-start/drift filter gates. (3) Eliminate cold-start City Hall queries on appear; implement deferred auto-scan upon acquiring first valid GPS fix. (4) 150m walking drift auto-refresh with manual refresh (↻) support and cold-start loading status. | **S** | ✅ Done |
| **J.15** | **WJ15-STATION-MARKER-TOGGLE** | **Subway Station Marker Style Pipeline & Sub-Fog Bullets Layer:** (1) Wire `SubwayStationMarkerStyle` (`Explored Only`, `All Stations`, `Hidden`) from Settings through `POIMaskManager.resolvePhase`. (2) Add `subway-station-bullets-layer` beneath the fog mask for subtle orienting sub-context in `All Stations` mode with theme-adaptive styling. (3) Lower `archiveLayer.minimumZoomLevel` to `11.0` and polish 4.5pt circle styling. (4) Add tap-to-inspect interactivity for archive and sub-fog bullets to open `TransitRevealSheet`. (5) Unit tests in `POIMaskingTests` and `CustomizationSettingsTests`. | **S** | ✅ Done |
| **J.16** | **WJ16-SPARKLINE-CONTRAST** | **Transit Sparkline Readability & Arrival Contrast Fix:** (1) Switch arrival countdown minutes in `TransitRevealSheet` from `Color(hex: "#FFB300")` to `.primary` for WCAG AA contrast in Day Mode — LIVE badge stays amber (status signal, not body text). (2) Replace `"7 Days Ago"` / `"Today"` X-axis labels in `TransitSparklineView` with dynamic abbreviated day names + short dates (e.g., `"Thu 8/14"` → `"Wed 8/20"`) computed backwards from a `referenceDate: Date = Date()` init parameter. (3) Add today's headway value as a small numeric label near the trailing glowing dot. (4) Keep 48pt chart height, existing avg header badge, existing dashed midline. (5) Pin `referenceDate` in `TransitRevealSheetTests` snapshot to avoid daily CI drift. **Explicitly out of scope:** NearbyBusesCapsule (contrast acceptable), adaptive color token system (overkill), tap-to-timetable affordance (no timetable to open), formal Y-axis labels (chart too compact), min/max annotations (collision risk at adjacent data points). | **S** | ✅ Done |

### 🐛 Post-Wave J Field Fixes & UX Polish (Wave K)

> [!NOTE]
> **Field testing results.** Issues discovered during hands-on testing after Wave J completion. P0 items block core interactivity; P1 items affect data correctness; P2/P3 items are polish and architectural planning.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **K.1** | **WK1-TAP-TARGETS** | **Invisible 44pt Transit POI Tap Targets (Visual Size Preserved):** Widen `visibleFeatures` hit-test from single-point query to `CGRect(x: p.x-22, y: p.y-22, width: 44, height: 44)` tolerance box with closest-feature distance tiebreaking. Preserve refined `4.5pt` archive circle radii and sub-fog bullet styling — zero visual changes. Applies to `activeLayerId`, `archiveLayerId`, `subwayStationBulletsLayerId`, and `nearbyBusStopsLayerId` layers in `MapView.Coordinator.handleMapTap`. | **S** | ✅ Done |
| **K.2** | **WK2-RELIABILITY-PICKER** | **Dual-Input Reliability Inspector (Passive Matrix + Day/Hour Menu):** Add compact `Menu`-based Day and Hour selectors below `ReliabilityHeatmapCanvas` as the primary entry point to `TransitMatrixInspectorView`. Keep the 24×7 Canvas as a passive high-density visual overview with optional crosshair highlight on touch. Each picker selection opens the inspector sheet with the corresponding `HourlyReliabilityRecord`. Eliminates reliance on 5.2pt-tall cell tap targets. | **S** | ✅ Done |
| **K.3** | **WK3-MULTI-ROUTE-SHEET** | **Multi-Route Transit Sheet for Co-Located Lines (R/W, N/Q, B/D/F/M):** (1) Expand `StopDetails` to carry `routeIds: [String]` for all lines serving the station. (2) Render stacked horizontal line badge circles in the sheet header (e.g., `[N] [Q] [R] [W]`). (3) **Live Arrivals:** Unified chronological stream across all sibling routes from shared GTFS-RT feed, with individual line badges per arrival row. (4) **Full Timetable:** Add horizontal route pill selector strip (`[All] [R] [W]`) to filter `DepartureMatrixView` by specific line. Ensure `TransitRealtimeService.parseFeedMessage` does not filter out sibling `tripRouteId` values. | **M** | ✅ Done |
| **K.4** | **WK4-BUS-TERMINAL-NAMES** | **Bus Terminal Name Resolution via Multi-Tier SQLite Hierarchical Lookup:** In `fetchStopDetails`, implement 3-tier name resolution: (1) Primary `stop_name` from `transit.stops WHERE stop_id = ?`. (2) If empty/generic, join `parent_station` and pull parent `stop_name` (`SELECT stop_name FROM transit.stops WHERE stop_id = (SELECT parent_station ...)`). (3) Offline cross-street intersection fallback from local spatial DB if both tiers fail. Verify `derivee_transit.sqlite` hydration pipeline populates terminal-level `stop_name` and `parent_station` columns correctly. | **S** | ✅ Done |
| **K.5** | **WK5-DIRECTION-GREYING** | **Grey Out Unavailable Direction in Full Timetable:** In `DepartureMatrixView`, check `timetableSchedule` for records in each `directionId`. If a direction has zero scheduled departures, render its segmented control tab as `.disabled(true)` with `.opacity(0.35)` and `(No Service)` label suffix. Auto-select the valid direction on appear. Prevent user selection of empty direction tabs. | **XS** | ✅ Done |
| **K.6** | **WK6-EDGE-HARDENING-AND-MULTI-CITY-RFC** | **Immediate Camera Edge Hardening & Multi-City Architecture RFC:** (1) **Immediate fix:** Tighten `CameraBounds.rubberBandMargin` from `0.35` (~38km) to `0.05` (~5km) with fast `.easeOut` rollback damping, making edge-peeking past the fog shadow impossible. Zero performance cost (single float constant change). (2) **Architecture RFC:** Author `docs/multi-city.md` specifying: GPS-triggered city detection via `CLGeocoder.reverseGeocodeLocation`, user prompt flow ("Exploring [City]? Download Transit Pack"), Cloudflare R2 city pack bundle format (`transit.sqlite` + `subway.geojson` + `city_config.json`), dynamic `CameraBounds` loading from active city pack, and `ATTACH DATABASE` mounting strategy for per-city transit databases. **Implementation deferred to Wave L.** | **M** | ✅ Done |
| **K.7** | **WK7-PAST-DEPARTURES-FILTER** | **Filter Departed Trains & Multi-Hour Timetable Live Reconciliation:** (1) **Absolute-Minute Live Reconciler:** Replace flawed `(currentMinute + arr.minutes) % 60` modulo math in `DepartureMatrixView.reconciledRecords` with absolute minutes-from-midnight 1-to-1 matching across active and upcoming hours (current hour + next 1–2 hours, $\pm 10\text{m}$ tolerance), eliminating cross-hour wrapping bugs where a 12:03 train attached a `37m` badge to an 11:03 past departure. (2) **Past Departure Dimming:** Visually dim departed schedule pills (`hour < currentHour` or `hour == currentHour && minute < currentMinute`) with `.opacity(0.35)` and disabled styling. (3) **Delay-Aware Liveness:** Delayed departures with past scheduled times remain active in the timetable with live $\Delta t$ and delay badges (`+Xm`) until their estimated live arrival time passes ($t_{\text{estimated}} < t_{\text{now}}$). (4) **30s Boarding Grace Period & Live Pruning:** In `TransitRealtimeService.parseFeedMessage` and `TransitRevealSheet`, maintain trains at platform as `0m / Boarding` for 30s ($0 \le \text{elapsed} < 30\text{s}$), then immediately purge ($\text{diffSec} < -30\text{s}$). (5) **Dynamic Live Pill Injection:** Dynamically insert unmatched ad-hoc GTFS-RT arrivals into their corresponding hour row with amber live borders. (6) Comprehensive unit and snapshot tests in `TransitRevealSheetTests`. | **S** | ✅ Done |
| **K.8** | **WK8-BUS-FALLBACK-HEADWAYS** | **Bus-Aware Headway & Directional Fallback Realism (B32 & One-Way Corridors):** (1) Map B32 terminals (*Long Island City - Queens Plaza* NB / *Williamsburg Bridge Plaza* SB) and Williamsburg bus routes in `TransitRealtimeService.resolveBusDestination`. (2) In `SpatialDatabaseManager.generateFallbackTimetable`, distinguish bus routes from subways: replace 3m rush-hour bursts (12–16 dep/hr) with realistic 15–30m bus headways (2–3 dep/hr for local routes, 4–7 dep/hr for SBS/trunks). (3) Add one-way corridor detection for Kent Av / Wythe Av in `generateFallbackAvailableDirections` to eliminate phantom reverse-direction departures on one-way streets. (4) Align `generateFallbackHeadways` (15–25m) and `generateFallbackHourlyReliability` with bus metrics. | **S** | ✅ Done |

### 🌍 Multi-City Architecture (Wave L)

> [!NOTE]
> **Gated on:** Wave K.6 (`docs/multi-city.md` RFC) shipped. Wave L is architected across **4 parallel execution tracks** (Tracks A, B, C, D) that can be developed concurrently across sub-agents / developers.
> - **Track A (Tooling/Backend):** Go / Python CLI & GIS data compilers (independent of Xcode).
> - **Track B (Storage/Core):** iOS GRDB SQLite hot-swap barrier, dynamic bounds, and H3 coordinate routing.
> - **Track C (UI/UX):** SwiftUI prompt sheets, city selector, settings storage manager, and timetable bitmask navigators.
> - **Track D (Cartography):** MapLibre dynamic GeoJSON loaders, multi-modal casing styles, and fog cache handshake.
> 
> First non-NYC target city: **Boston (MBTA)** with static multi-modal GTFS (Subway, Green Line LRT, Silver Line BRT, Harbor Ferries). Multi-city asset distribution is engineered for 100% free-tier compliance across Cloudflare R2 and Oracle Cloud up to 100+ metros (see [docs/multi-city.md §7.3](file:///Volumes/T7ssd/derivee/docs/multi-city.md#73-free-tier-capacity--100-city-scaling-analysis)).

#### Track A: Data & GIS Compilation Tooling (Go / CLI / Offline)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **L-A.1** | **WLA1-GTFS-COMPACTION** | **Static GTFS 14-Day Calendar Compactor, Station Resolution & Pre-Compiled Statistics:** (1) Implement `scheduled_hourly_patterns` schema in `transit.sqlite` with `service_mask uint16` ($\pm 7\text{d}$ rolling window relative to compilation anchor $T_0$, bit 7 = today), `baseline_days_of_week uint8` (7-bit fallback for stale packs), `minute_offsets` string, and single `idx_patterns_lookup` index, shrinking DB size from 110MB to **3.8MB uncompressed** (<1.2MB .zst, 0.12ms fetch latency). (2) Build `stop_resolution` table (`WITHOUT ROWID`) with reflexive transitive closure across GTFS `stops.txt` parent-child hierarchy for $\mathcal{O}(1)$ platform disambiguation and reverse lookup. (3) Universal build-time distance-based linear interpolation for `timepoint = 0` and headway expansion (`frequencies.txt`). (4) **Pre-compiled query optimizer statistics:** Observer runs `PRAGMA analysis_limit = 1000; ANALYZE; PRAGMA optimize(0x10000); VACUUM;` embedding `sqlite_stat1` into distributed `transit.sqlite` before Zstandard compression. (5) Define `city_config.json` schema and `cities.json` remote manifest format. (6) Package default bundled NYC pack (`city-nyc.pack.zst`). | **M** | ✅ Done |
| **L-A.2** | **WLA2-ARC-TOPOLOGY** | **Planar Arc-Topology Visvalingam-Whyatt Simplification Engine:** (1) Build topology graph from GTFS `shapes.txt` locking junction nodes ($A = \infty$) at route intersections and terminals. (2) Simplify each Arc via iterative elimination of smallest effective triangle area with mode-adaptive thresholds ($\approx 10^{-9}\text{ deg}^2$ subway, $5 \times 10^{-9}$ LRT, $10^{-8}$ ferry, $5 \times 10^{-8}$ commuter rail), guaranteeing zero Z-fighting across overlapping routes in MapLibre. (3) Emit normalized `transit-lines.geojson` for bundled NYC pack. | **M** | ✅ Done |
| **L-A.3** | **WLA3-GIS-BRIDGES** | **GIS OSM Pedestrian Bridge Preservation Pipeline:** (1) Implement boolean geographic subtraction script: $\text{Walkable\_Mask} = (\text{Neighborhood} \setminus \text{Water}) \cup \text{Pedestrian\_Bridges}$. (2) Query OpenStreetMap for pedestrian-accessible bridges (`bridge=yes` with `highway=footway | cycleway | pedestrian | primary/secondary with sidewalk`) and re-add them after water subtraction to ensure 100% completion is possible for waterfront neighborhoods. | **S** | ✅ Done |
| **L-A.4** | **WLA4-BOSTON-MBTA-PACK** | **First Non-NYC Multi-Modal Pack: Boston (MBTA) & OCI Ingestion:** (1) Ingest MBTA static GTFS via Observer with 3-tier delta checking. (2) Build `transit.sqlite` (Subway Red/Orange/Blue + Green Line LRT `frequencies.txt` expansion + Silver Line BRT + Charlestown/Hingham Ferries). (3) Build `stop_resolution` and pre-compile `sqlite_stat1`. (4) Generate `transit-lines.geojson` via Arc-Topology engine. (5) Author `city_config.json` for Boston metro envelope with structured `realtimeEndpoints` for MBTA multi-feed (subway, bus, Green Line, ferry). (6) OSM bridge ingestion (Longfellow, Harvard, Zakim). (7) Package `city-bos.pack.zst`, upload to Cloudflare R2, update `cities.json` manifest. | **M** | ✅ Done |

#### Track B: iOS Core Storage & Spatial Engine (Swift / GRDB / H3)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **L-B.1** | **WLB1-CITY-PACK-MANAGER** | **`CityPackManager` & Zstd Unpacking Engine:** Implement `CityPackManager` in Swift: Zstandard decompression via native `libcompression`, atomic extraction to `~/Documents/CityPacks/{slug}/`, integrity verification via SHA-256 hash, remote `cities.json` manifest fetcher, and first-launch unpack of bundled `city-nyc.pack.zst` (<200ms, zero network dependency). | **M** | ✅ Done |
| **L-B.2** | **WLB2-DYNAMIC-BOUNDS-MIGRATION** | **Dynamic Camera Bounds, Schema Migration & Land-Only Water Fog:** (1) Refactor `CameraBounds` to load bounds dynamically from active `CityConfig`. (2) Create per-city hex tables (`explored_hexes_{slug}`) in `SpatialDatabaseManager`. (3) Zero-downtime schema migration: `ALTER TABLE explored_hexes RENAME TO explored_hexes_nyc;` on upgrade. (4) Wire `SpatialStore` to read from active city's hex table via dynamic table name. (5) Dynamic fog bounding polygon from `activeConfig.bounds`. (6) **Strict Land-Only Fog Policy** with quiet water gliding: GPS puck glides over dark water during ferry crossings, clearing fog at piers and landmass only. | **M** | ✅ Done |
| **L-B.3** | **WLB3-TRANSIT-HOT-SWAP** | **Coordinated Two-Phase Transit DB Hot-Swap & Coordinate-Routed Tracking:** (1) Implement **Coordinated Two-Phase Barrier**: Phase 1 (`prepareForCitySwap`) cancels `TransitRealtimeService` feed polling, pauses `NearbyBusesCapsule` spatial scans, auto-dismisses `TransitRevealSheet`. Phase 2 invokes `dbPool.releaseMemory()` to drain prepared statements across reader connections, then executes `DETACH DATABASE transit` + `ATTACH DATABASE` inside serial GRDB `dbWriter.writeWithoutTransaction` write barrier, preventing iOS `0xdead10cc` background termination. (2) Post-swap `PRAGMA transit.optimize;` to warm optimizer stats. (3) **Coordinate-routed background tracking:** live GPS fixes route to the correct `explored_hexes_{slug}` table based on bounding-box math regardless of which city is actively viewed in the UI. | **M** | ✅ Done |

#### Track C: SwiftUI UI & Experience Components (SwiftUI / State)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **L-C.1** | **WLC1-CITY-PROMPT-SHEET** | **GPS City Detection, Download Prompt Sheet & Silent Auto-Switch Toast:** (1) Fast offline bounding-box check against cached `cities.json` + `CLGeocoder` fallback for ambiguous zones. (2) Non-blocking `CityDownloadPromptSheet` bottom sheet with byte counter progress, `[ Download Now ]` / `[ Not Now ]`, and 7-day nag snooze. (3) Silent auto-switch 3s toast for installed packs (*"Welcome to Boston • Switched active city"*). (4) Fallback ambient tracking for declined/offline cities under generic fog envelope. | **M** | ✅ Done |
| **L-C.2** | **WLC2-CITY-SELECTOR-STATS** | **Screen 3 Multi-City Selector & All Metros Summary:** (1) Screen 3 frosted-glass menu pill (`[ 🟢 NYC ▾ ]`) with per-city stats, leaderboard, and journal milestones. (2) "All Metros Summary" lifetime totals mode. (3) "View on Map" city switch action. (4) Smart multi-city GPX import with bounding-box auto-partitioning into `explored_hexes_{slug}` in a single SQLite transaction. | **M** | ✅ Done |
| **L-C.3** | **WLC3-STORAGE-MANAGER** | **`Settings > Cities & Storage` Manager:** (1) Installed vs available cities breakdown. (2) Transparent disk footprint breakdown (compressed download vs uncompressed on-disk size). (3) Update available badges when newer timetables publish to R2. (4) **Decoupled deletion:** deletes static assets (`~/Documents/CityPacks/{slug}/`) while permanently preserving `explored_hexes_{slug}` in SQLite. (5) NYC Core Metro deletion protection. (6) Dynamic agency attributions from `city_config.json`. | **M** | ✅ Done |
| **L-C.4** | **WLC4-TIMETABLE-RECONCILER** | **$\pm 7$ Day Timetable Navigation, Circular Distance Reconciler & ScheduleRelationship Badges:** (1) Bitwise future day evaluation in `DepartureMatrixView` via `(service_mask & (1 << day_offset)) != 0` (<0.12ms). (2) **Circular Modular Distance Matching** for live GTFS-RT Protobuf arrivals across midnight boundaries ($\tau = 10\text{m}$ rail/ferry, $\tau = 15\text{m}$ bus). (3) Past days show Observed Reality Replay from `stop_events` (or honest scheduled fallback with banner). (4) **GTFS-RT `ScheduleRelationship` Visual Treatments:** In-situ rendering for `CANCELED` (0.4 opacity strikethrough + red badge), `ADDED` (bold + accent badge), `UNSCHEDULED` (italic + LIVE pulse), `DUPLICATED` (merged). | **M** | ✅ Done |

#### Track D: MapLibre Cartography Layer (Swift / MapLibre Native)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **L-D.1** | **WLD1-GEOJSON-LOADER** | **Dynamic `MLNShapeSource` Multi-Modal Transit Loader:** (1) Retire static `MtaSubwayNetworkData.swift`. (2) Implement dynamic `MLNShapeSource` line loader in `MapView.Coordinator` reading `transit-lines.geojson` from the active city pack. | **M** | ✅ Done |
| **L-D.2** | **WLD2-MODAL-STYLING** | **4-Tier Extended GTFS (HVT) Modal Cartography & Badging:** (1) Modal normalization into 4 visual styling priorities: Subway & PATH (6px light silver casing + 4px line), Light Rail (4px line + dashed casing), BRT/Bus capillary lens ($z \ge 14.5$), Maritime Ferry (2.5pt dashed cyan line over water with pier landing pins). (2) Multi-modal capsule badging in `TransitRevealSheet` data-driven from `transit.routes`. | **S** | ✅ Done |
| **L-D.3** | **WLD3-FOG-CACHE-HANDSHAKE** | **MapLibre Fog Mesh Cache Invalidation & Viewport Handshake:** Assign newly initialized `MLNShapeCollectionFeature` with updated coordinate arrays on city switch (preventing GPU mesh reuse), coordinated synchronously on `@MainActor` with camera transition (`mapView.setCenter`) to eliminate intermediate rendering glitches. | **S** | ✅ Done |

### 🧭 Navigation Ergonomics & Realtime Ground Truth (Wave M)

> [!NOTE]
> **Post-Wave L Field Hardening.** Resolves realtime status fidelity at trip terminals, sheet scroll priority conflicts, ambient dismissal inconsistencies, and unifies spatial orientation controls into a bottom-right cluster.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **M.1** | **WM1-TERMINUS-DWELL** | **GTFS-RT Vehicle Stop Status & Terminus Dwell Ground Truth:** Cross-reference `VehiclePosition.VehicleStopStatus` (`STOPPED_AT`, `INCOMING_AT`, `IN_TRANSIT_TO`) and initial stop sequence in `TransitRealtimeService`. Render dwelling vehicles at initial origin terminal as `"At Terminus"` / `"Scheduled"` instead of active `"x stops away"` until the vehicle physically transitions to `IN_TRANSIT_TO` or passes sequence 1. | **S** | ✅ Done |
| **M.2** | **WM2-SHEET-SCROLL-PRIORITY** | **Sheet Scroll Interaction & Hour Picker Gesture Disambiguation:** Apply `.presentationContentInteraction(.scrolls)` and `.scrollBounceBehavior(.basedOnSize)` to `TransitMatrixInspectorView`, `TripLedgerView`, and inspector sub-sheets. Prevent native bottom sheet interactive drag-to-dismiss from intercepting vertical swipes on scrollable reliability metrics and timetable views. | **XS** | ✅ Done |
| **M.3** | **WM3-AMBIENT-MAP-DISMISSAL** | **Ambient Map Dismissal Contract ("Map is the UI" for Overlays):** In `MapView.Coordinator.handleMapTap`, handle ambient taps outside transit POI features (`closest == nil`) by collapsing active floating lenses (`NearbyBusesCapsule`) and dismissing transient toast banners. Auto-collapse quick lens capsules on map gesture pan/drag (`mapViewRegionWillChange`). | **S** | ✅ Done |
| **M.4** | **WM4-ORIENTATION-CLUSTER** | **Bottom-Right Navigation Orientation Cluster:** Relocate native MapLibre compass needle to `.bottomRight` directly stacked above the 50×50 `RecenterFAB` (`x: 20pt`, `y: 102pt + safeAreaInsets.bottom`). Preserves 120Hz native bearing animation, auto-fade when North-Up, and tap-to-reorient with brand-aligned `ApertureCompassNeedle`. | **XS** | ✅ Done |

### 🏙️ Multi-City Pack Hardening & Global Fog Architecture (Wave M.5)

> [!NOTE]
> **Field Audit & Experience Polish.** Resolves fog bounding box rectangular cutoffs with a global world mask, eliminates single-city neighborhood data leakage during remote city switching, compiles and attaches per-city neighborhood exploration boundaries, deep-links city management navigation, and streamlines 1-tap city pack downloads with reactive state synchronization.
>
> **Modular Agent Scope:** Each sub-wave is self-contained with dedicated test coverage to allow isolated agent auditing.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **M.5.1** | **WM5.1-GLOBAL-FOG** | **Global World Fog Mask Architecture (World Polygon & Seamless Apertures):** Replace localized 30km×30km per-city bounding box with a global world outer polygon `[(-85.0511, -179.999), (85.0511, -179.999), (85.0511, 179.999), (-85.0511, 179.999), (-85.0511, -179.999)]` in CW winding order. Explored hexes anywhere on earth dissolve into interior aperture cutouts with golden illuminated borders. Eliminates artificial rectangular fog edges and border lines across city margins. | **S** | ✅ Done |
| **M.5.2** | **WM5.2-CITY-SWITCH-NAV** | **Multi-City Navigation & Center Coordinate Disambiguation:** Ensure switching active city (or tapping "View on Map") targets `config.center.coordinate` (e.g. Boston Common `42.3601, -71.0589`) instead of clamped out-of-bounds coordinates. Graceful empty state handling for cities without compiled neighborhood boundaries. | **S** | ✅ Done |
| **M.5.3** | **WM5.3-MANAGE-CITIES-DEEPLINK** | **"Manage Cities" Deep-Link & Direct Storage Navigation:** Update "Manage Cities..." in `CitySelectorPill` to navigate directly to `CitiesStorageManagerView` (or deep-link via `ScrollViewReader` with `#citiesStorage` anchor in `SettingsView`) instead of landing on top of general Settings. | **XS** | ✅ Done |
| **M.5.4** | **WM5.4-CITY-CATALOG-DISCOVERY** | **City Pack Cloud Catalog Discovery, 1-Tap Download & State Sync:** Add 1-tap download buttons with live progress on uninstalled cards in `AllMetrosSummaryView` and "Available to Download" menu items in `CitySelectorPill`. Call `CityDetectionService.markCityInstalled` upon download completion in `CitiesStorageManagerView` for instant state synchronization across all selectors. Add "Switch Active Metro" button in `CitiesStorageManagerView`. | **S** | ✅ Done |
| **M.5.5** | **WM5.5-MULTI-CITY-NEIGHBORHOODS** | **Multi-City Neighborhood GIS Compiler, Pack Bundling & Dynamic Attachment:** (1) Ingest Boston neighborhood boundaries via `scripts/generate_neighborhood_stats.py` with boolean water clipping & bridge preservation (`Walkable_Mask = (Neighborhood \ Water) ∪ Bridges`). (2) Package `neighborhood.sqlite` (or embed `neighborhood_stats` / `neighborhood_hexes` tables into `transit.sqlite`) in `city-{slug}.pack.zst`. (3) Wire `SpatialDatabaseManager` to dynamically mount active city neighborhood data during the two-phase swap barrier. (4) Render live per-city neighborhood exploration progression in `StatsView`. | **M** | ✅ Done |


### 🚀 On-Device Multimodal Routing & Reliability Engine (Wave N)

> [!NOTE]
> **Gated on:** Wave M (field hardening) complete. Wave N introduces an **on-device C++ Hybrid RAPTOR routing engine** with Swift interop, ULTRA precomputed transfer shortcuts, real-time GBFS micro-mobility integration, Observer historical slot profiling, 120Hz immediate-mode Canvas, and multimodal cognitive UX architecture.
>
> **Parallel Execution:** Wave N is architected across **4 parallel tracks** (A, B, C, D) that can be developed concurrently:
> - **Track A (Observer / Go / CLI):** OSM PBF extraction, ULTRA shortcut precomputation, RAPTOR binary compilation, Service Alerts ingestion & 15-min origin dispatch slot profiling.
> - **Track B (C++ Engine / Swift):** Swift-C++ interop scaffold, Hybrid RAPTOR core algorithm (0–45m real-time active trains + frequency expansion beyond), dynamic disruption bitmask, Pareto evaluation.
> - **Track C (Dynamic Data / GRDB):** GBFS station sync service & multi-modal dock gating, `service_disruptions` & `trip_slot_profiles` integration, circular midnight delay math.
> - **Track D (SwiftUI / Canvas / Multimodal UX):** Predictive route comparison cards with confidence bands, Train Inspector sheet, Smart Zoom station badges, 120Hz Canvas, and Doc 14 UX tightening (3-tier GTFS-RT confidence, subterranean exit mapping, train car positioning, landmark walking guidance, dynamic GBFS dock gating, 15%/50%/90% bottom sheet ergonomics).

#### Track A: Observer Data Compilers & Extraction (Go / Python)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **N-A.1** | **WNA1-OSM-EXTRACTOR** | **Go Observer OSM PBF Quantized Graph Extractor:** Stream PBF nodes/ways with `github.com/qedus/osmpbf`. Quantize coordinates to fixed-point `int32` ($\times 10^7$). Strip OSM metadata. Extract walkable/cycleable network into compact adjacency list with `EdgeFlags` (bitmask for Walkable, Wheelchair, Steps, Elevator). Emit `walk_graph.bin` (<25 MB). | **M** | ✅ Done |
| **N-A.2** | **WNA2-ULTRA-SHORTCUTS** | **ULTRA Transfer Shortcut Precomputer:** Bounded one-to-many Dijkstra over quantized walk graph ($d_{\max} = 1.2\text{km}$). Compute Pareto-optimal transfer shortcuts between transit stops. Output Compressed Sparse Row (CSR) `ultra_transfers.csr` (`stop_offsets uint32`, `target_stop uint32`, `distance_m uint16`, `flags uint8`). | **L** | ✅ Done |
| **N-A.3** | **WNA3-RAPTOR-COMPILER** | **Binary RAPTOR Timetable Compiler (`timetable.bin`):** Compile GTFS static into zero-copy AoS packed timetable binary: `StopTime` (12B), `Trip` (8B), `Route` (12B), `Stop` (20B), `Transfer` (8B). 128-byte header with magic `0x44525631` (`DRV1`), 16 KiB page-aligned sections. Total NYC MTA footprint ~17.58 MB. | **M** | ✅ Done |
| **N-A.4** | **WNA4-ALERTS-SLOTS** | **Observer Historical Profiling & GTFS-RT Service Alerts Ingestion:** (1) Service Alerts ingestion storing disruption intervals with route/stop bitmasks. (2) 15-minute origin dispatch slot profiling calculating scheduled headway ($SWT$), observed headway ($AWT$), excess wait time ($EWT$), and on-time performance ($OTP$) over rolling 168h windows. Output `trip_slot_profiles` table. | **M** | ✅ Done |
| **N-A.5** | **WNA5-CITY-PACK-V2** | **City Pack v2 Format Extension:** Update city pack directory structure to include `timetable.bin`, `ultra_transfers.csr`, `walk_graph.bin` alongside existing `transit.sqlite` and `transit-lines.geojson`. Bump `city_config.json` version to `2`. Regenerate and upload NYC + Boston packs to R2. | **M** | ✅ Done |
| **N-A.6** | **WNA6-WALK-GRAPH-CSR** | **Offline Walk Graph CSR Compiler & R-Tree Spatial Index:** Precompute pedestrian walk graph into forward CSR layout (`walk_offsets.bin` + `walk_edges.bin`) and FlatGeobuf/packed R-Tree (`walk_rtree.bin`) in Go Observer. Zero heap allocation during Dijkstra exploration. | **M** | ✅ Done |

#### Track B: On-Device C++ Routing Core & Swift Interop (C++20 / Swift)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **N-B.1** | **WNB1-CPP-INTEROP-SCAFFOLD** | **Swift C++20 Interop Scaffold & Memory-Mapped Timetable Reader:** Create `DeriveeCore` framework target with C++20 standard (`-cxx-interoperability-mode=default`). Zero-copy `mmap` loader for `timetable.bin` and `ultra_transfers.csr` validating 128-byte headers. Native Swift wrapper types with direct C++ struct access. | **M** | ✅ Done |
| **N-B.2** | **WNB2-HYBRID-RAPTOR-CORE** | **Hybrid RAPTOR Core Algorithm:** Standard McRAPTOR round-based Pareto exploration ($K \le 4$ rounds). 0–45 min lookahead: scan live active vehicle positions with Euclidean modulo delay arithmetic. 45+ min lookahead: expand frequency intervals from `scheduled_hourly_patterns`. Scan CSR `ultra_transfers` during round relaxation. Bounded runtime: <15ms on Apple Silicon. | **L** | ✅ Done |
| **N-B.3** | **WNB3-DISRUPTION-BITMASK** | **Dynamic Disruption Masking:** Apply active GTFS-RT service disruptions as bitmasks over routes/trips during RAPTOR route scanning. Instant skip for canceled trips (`ScheduleRelationship.CANCELED`) or suspended route segments with zero timetable re-allocation. | **S** | ✅ Done |
| **N-B.4** | **WNB4-PARETO-OPTIMIZATION** | **4D Pareto Optimization & Profile Evaluator:** Multi-criteria journey evaluation across 4 dimensions: (1) Arrival time $\tau$, (2) Number of transfers $k$, (3) Total walking/biking distance $d_{\text{walk}}$, (4) Journey Reliability Score $JRS = \sum \log(1 + EWT_i)$. Return non-dominated Pareto frontier journeys to Swift. | **M** | ✅ Done |
| **N-B.5** | **WNB5-BOUNDED-WALK-ROUTER** | **Bounded One-to-Many Walk Router:** Bounded A* / Dijkstra over quantized walk graph for initial (origin $\to$ nearby stops, $r \le 800\text{m}$) and final (destination stops $\to$ target, $r \le 800\text{m}$) transfer legs. Prune search with Euclidean distance heuristic. Budget: <3ms execution. | **M** | ✅ Done |
| **N-B.6** | **WNB6-SWIFT-ROUTING-BRIDGE** | **`RoutingEngineBridge` & Journey Planner Actor:** `@Observable` Swift actor managing C++ engine lifecycle, thread-safe background execution at `.userInitiated` priority, and translating Pareto result journeys into rich `JourneyItinerary` Swift models. | **M** | ✅ Done |
| **N-B.7** | **WNB7-ENERGY-EVALUATOR** | **Micro-Climate & Thermal Comfort Energy Weighting ($\text{PET}$ Model):** Integrate building shadow polygons and tree canopy cover into walking edge weights. Dynamic summer shaded vs winter sunlit route options. | **S** | ✅ Done |

#### Track C: Dynamic Data Layer (Swift / GRDB)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **N-C.1** | **WNC1-GBFS-SYNC** | **`GBFSSyncService` & Multi-Modal Dock Validation:** Implement `GBFSSyncService` with `Task`-based 30s async polling. Ephemeral `gbfs_cache.sqlite` (`DatabaseQueue`, `.userInitiated` QoS). Dynamic 3-tier dock availability gating product ($G(e) = g_{\text{pick}} \cdot g_{\text{drop}}$): $>3$ low risk, 1–2 moderate risk (pre-arm secondary fallback dock), 0 high risk (auto-reroute to dock within 3m walk). Staleness gate: 600s. | **M** | ✅ Done |
| **N-C.2** | **WNC2-RELIABILITY-ENGINE** | **`TransitDatabaseEngine` & Slot/Disruption Queries:** Implement async GRDB queries for `service_disruptions` (time-bounded calendar work) and `trip_slot_profiles` (15-min origin dispatch stats). Aggregates $SWT$, $AWT$, $EWT$, and $OTP$ over 168-hour rolling windows. | **M** | ✅ Done |
| **N-C.3** | **WNC3-CIRCULAR-TIME** | **`GTFSMidnightResolver` & Euclidean Modulo Circular Distance:** Implement `euclideanModulo(a, n) = (a % n + n) % n` for non-negative modular arithmetic. `calculateSignedCircularDelay` mapping GTFS-RT timestamps to signed $[-720, +720]$ minute offsets on the 1,440-minute clock face. `GTFSRealtimeStreamProcessor` actor for processing `StopTimeUpdate` payloads with midnight-safe delay computation. | **S** | ✅ Done |

#### Track D: UI, Visualization & Multimodal UX (SwiftUI / Canvas / Metal)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **N-D.1** | **WND1-DEPARTURE-CANVAS** | **120Hz Immediate-Mode `DepartureMatrixCanvas`:** Metal-backed `Canvas(rendersAsynchronously: true)` + `.drawingGroup()`. Flat contiguous `[Float]` buffer (4,320 elements: $24\text{h} \times 60\text{m} \times 3$ percentiles). Index stride: $\text{Offset}(h, m, p) = (h \times 60 + m) \times 3 + p$. Direct RGBA interpolation avoiding SwiftUI modifier chains. Target: 0.8ms–1.9ms per frame (< 8.33ms 120Hz budget). | **M** | ✅ Done |
| **N-D.2** | **WND2-ROUTE-CARDS** | **Predictive Route Comparison Cards, 3-Tier Confidence Badges & Multi-Profile Selector:** `JourneyItinerary` result cards showing arrival time with P10–P90 confidence intervals, 3-tier GTFS-RT confidence badges (`Verified` live pulsing dot, `Estimated` stale tag, `Static` schedule fallback), total cost/distance/time summaries per mode, transfer count badges, walking/biking effort, and inline disruption callouts. Multi-profile selector (`[Most Reliable]`, `[Fastest]`, `[Fewest Transfers]`, `[Step-Free]`, `[Multi-Modal Bike + Rail]`). | **M** | ✅ Done |
| **N-D.3** | **WND3-CONFIDENCE-BANDS** | **P10/P50/P90 Confidence Interval Visualization:** Render percentile wait-time bands from `[Float]` buffer using `Canvas` or `Path`-based area fills. Statistical variance disutility coloring: narrow bands (reliable, green tint) vs wide bands (unreliable, amber/red tint). Tap-to-inspect detail overlay. | **S** | ✅ Done |
| **N-D.4** | **WND4-TRAIN-INSPECTOR-SMART-ZOOM** | **Deep Train Inspector, Car Exit Badges & Smart Zoom Station Bullets:** (1) Train Inspector sub-sheet on tapping live arrival row (Track Thermometer, GTFS-RT `occupancy_status` / crowd fallback, 15-min slot regularity card, map synchronizer). (2) Platform train car recommendation badges (`Board near front/middle/rear car` for transfer & egress alignment) and deterministic subterranean exit codes (e.g. `Exit 4B - NW Corner 42nd & Broadway`). (3) Smart Zoom MapLibre station symbol layer resolving discrete route bullets (`[4][5][6]` vs `[6]`) at $z \ge 14.5$. *Note: Inspector UX revised in Wave PA — car positioning and exit codes relocated to Screen 4C Active Navigation. Inspector streamlined to 5-element first-principles spec per design.md §10.5.* | **M** | ✅ Done |
| **N-D.5** | **WND5-THUMB-ZONE-DETENTS** | **Standardized 3-Tier Bottom Sheet Ergonomics & One-Handed Thumb Zone:** Standardize navigation sheets across 3 ergonomic detents: `15%` (Collapsed Peek for next upcoming step), `50%` (Half-Screen Step-by-Step Guidance), and `90%` (Full-Screen Expanded Timetable / Alternatives). Floating hero map viewport preservation. Primary action buttons (`Start Journey`, `Reroute`, `Unlock Bike`) anchored strictly in lower-third thumb zone. | **S** | ✅ Done |
| **N-D.6** | **WND6-LANDMARK-WALKING-DIRECTIONS** | **Landmark-Anchored Walking Guidance & Microclimate Shaded Routes:** Replace numerical distance-only prompts ("turn in 300ft") with 3–5 salient visual landmark anchors per walking route ("Turn left after the red brick pharmacy"). Multi-attribute cost weighting factoring urban tree canopy cover and building shadow vectors ($\text{PET}$ cost model) for shaded cool walking routes. | **M** | ✅ Done |
| **N-D.6.1** | **WND6.1-NATURAL-GUIDANCE-BLOCKS-LIGHTS** | **Natural Guidance Intermediate Reminders (City Blocks & Traffic Signals):** Add natural intermediate route reminders inspired by Apple/Google Maps Natural Guidance: replace raw numerical metrics with city block counts ("In 3 blocks, turn left...", "In 5 blocks...") and intersection/traffic signal framing ("At the next light, turn left...", "After the next light, bear right...", "Continue straight for 4 blocks past the park"). Dynamically transition guidance text across foresight, approach, and imminent decision zones during active walking navigation. | **S** | ✅ Done |
| **N-D.7** | **WND7-GBFS-DOCK-GATING-UI** | **Dynamic GBFS Dock Gating, E-Bike Battery SOC Overlays & Cycling Ergonomics:** Real-time 3-tier dock availability gating badges (>3 low risk, 1–2 moderate fallback pre-armed, 0 auto-reroute to nearest dock within 3m walk). E-bike state-of-charge (SOC %) overlays and usable range radiuses. Expanded 56×56pt touch targets and high-contrast WCAG AAA cycling HUD (0.5s glance window). | **M** | ✅ Done |
| **N-D.8** | **WND8-LIVE-DYNAMIC-RECOVERY** | **Live Dynamic Recovery & Multimodal Live Activity / Dynamic Island:** Automated missed-connection detection via live GPS vs platform departure timestamps. Instant 1-tap recovery card suggesting alternate bus/bike/walk legs. Native iOS Live Activity and Dynamic Island trip progress tracking with transfer countdowns, step transitions, and subterranean exit guidance. | **M** | ✅ Done |

### 🎯 Wave P: Transit UI Precision & Defect Remediation Sprint (Bugs 1–8)

> [!IMPORTANT]
> **Defect Remediation Focus:** Addresses the 8 high-priority functional and visual defects discovered during hands-on field testing. Prioritizes immediate correctness in the core departure matrix, arrival feeds, and sheet presentation without risking broader system instability.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **P.1** | **WP1-TIMETABLE-ANCHOR** | **Wall-Clock Matrix Anchoring & Temporal Invalidation (Bugs 1 & 2):** (1) Replace `ScrollViewReader` with iOS 17 `.scrollPosition(id: $scrollPositionID, anchor: .top)` on `ScrollView` + `.scrollTargetLayout()` on `LazyVStack`. (2) Enforce deterministic row heights (56pt row, 36pt header). (3) Headless initial state assignment with `transaction.disablesAnimations = true` to prevent collision with modal presentation animations. (4) Encapsulate temporal status badges in `TimelineView(.explicit(dates))` (Doc 18) to advance past departures and move "NEXT" badge without parent timetable tree invalidation. (5) Sync current-hour highlight strictly to wall clock. | **M** | ✅ Done |
| **P.2** | **WP2-MATRIX-DECLUTTER** | **Departure Matrix De-clutter & Single-Anchor Imminence (Bug 3):** Strip live arrival countdowns, duplicate pills, and pulsing dots from the static 24-hour reference matrix (`DepartureMatrixView`). Limit live countdown treatment exclusively to a single imminent departure anchor pill per route direction. Keep all high-cadence countdowns isolated to the top `LiveArrivalsCarousel` in `TransitRevealSheet`. | **S** | ✅ Done |
| **P.3** | **WP3-EXPRESS-DESTINATIONS** | **Express Destination Resolution & Terminal Endpoints (Bug 4):** Replace hardcoded route destination switches in `TransitRealtimeService.resolveDestination` with dynamic terminal inference (Doc 16): evaluate non-SKIPPED stops in GTFS-RT `StopTimeUpdate` in reverse sequence to resolve short-turns, inspect `NyctStopTimeUpdate.actual_track IN ('2', '3', 'M')` to detect physical express routing, and format disambiguated labels (e.g., `4 to Woodlawn EXP`). | **S** | ✅ Done |
| **P.4** | **WP4-PREFIX-PARSING** | **Bus Stop Name Prefix Parsing Hardening (Bug 5):** In Go Observer (`gtfs_static.go`), isolate directional bus stop tokens (`NB`, `SB`, `EB`, `WB`) using boundary-aware regex (`\b(NB|SB|EB|WB)\b`) so directional tokens are classified as routing qualifiers instead of binding into numbered street tokens (e.g. preventing `"Kent Av & NB 6 St"`). Disambiguate single-direction curbside stops in live timetable & arrivals carousel. | **XS** | ✅ Done |
| **P.5** | **WP5-TERMINUS-DWELL** | **Terminus Dwell Suppression & Anti-Ghost Movement (Bug 7):** Enforce terminal origin dwell anchor (Doc 17): when `is_assigned: true`, `stop_sequence <= 1`, and `status == STOPPED_AT`, clamp progress $\lambda \equiv 0.0$ and render `"At Terminus"` / `"Boarding"` instead of active `"x stops away"`. Flag `HOLDING_STATION` if departure is delayed $>120\text{s}$. | **S** | ✅ Done |
| **P.6** | **WP6-GLASS-CONTRAST** | **High-Contrast 3-Tier Glassmorphic Stack & Gesture Disambiguation (Bug 8):** (1) Replace bare `.ultraThinMaterial` in `.presentationBackground` with 3-tier compositing stack (Doc 18): `Color(uiColor: .systemBackground).opacity(0.84)` optical low-pass filter base + `.thickMaterial` diffusion + 0.5pt specular gradient border (>7.2:1 WCAG AAA contrast). (2) Apply `.presentationContentInteraction(.scrolls)` and `.presentationDragIndicator(.visible)` across all transit sub-sheets to eliminate gesture swallowing. | **S** | ✅ Done |

### 🎨 Wave PA: UI Alignment Sprint (Inspector Rearchitecture & Screen 4 Unblocking)

> [!IMPORTANT]
> **UI Alignment Gate:** This wave cleans up transit inspector UX debt (wordy headers, bus/train conflation, misplaced analytics) and unblocks orphaned navigation code (Screen 4B/4C) by adding the search entry point. Must complete before Wave Q (which depends on a clean station sheet) and Wave R (which adds kinematic markers to a clean inspector).

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **PA.1** | **WPA1-INSPECTOR-SPLIT** | **Inspector Split: GuidewayRunInspector + SurfaceRunInspector:** Split `TrainInspectorSheet.swift` into 2 files. Implement `SurfaceInspectableRoute` protocol for mode-specific cosmetic tokens (stop noun, water tracking, countdown display). Wire `TransitRevealSheet` to branch on `TransitModalClass`: `.subway`/`.lightRail` → `GuidewayRunInspector`; `.bus`/`.ferry` → `SurfaceRunInspector`. Remove all wordy headers (`PLATFORM EGRESS`, `TRACK THERMOMETER`), `Where to Board`, carriage diagram, regularity card. Spec: design.md §10.5, §10.6. | **M** | ✅ Done |
| **PA.2** | **WPA2-DISRUPTION-FOLLOWON** | **Inspector Data: Disruption Banner + Follow-On Departure:** Wire `fetchDisruptions(for: routeId)` from `service_disruptions` table into both inspectors as inline amber warning pill. Pass follow-on arrival timestamp from `LiveArrivalsCarousel` data as `Next [ route ] in X min` below the stop ladder. | **S** | ✅ Done |
| **PA.3** | **WPA3-RELIABILITY-BADGES** | **Station Sheet: Per-Line Reliability Badges:** Surface regularity data (from `trip_slot_profiles` 15-min origin dispatch slots) as compact per-route badges on Screen 2's arrival rows. Visual encoding: `● High` (green, OTP ≥90%), `◐ Moderate` (amber, 70–89%), `○ Variable` (red, <70%). Relocated from inspector. Spec: design.md Screen 2. | **S** | ✅ Done |
| **PA.4** | **WPA4-SEARCH-CAPSULE** | **Search Capsule Entry Point (Screen 4A):** Floating frosted-glass `.ultraThinMaterial` capsule on Screen 1 (top-center, below safe area). Tapping opens search view with offline prefix search against `transit.stops`, routes, and curated landmarks. Recent destinations via `@AppStorage`. Quick-access mode filters (`Subway`, `Buses`, `Rail`, `Saved`). Connect to existing `RouteComparisonListView` (Screen 4B) to unblock orphaned navigation code. Spec: design.md §12.2. | **M** | ✅ Done |
| **PA.5** | **WPA5-MAP-SYNC-POLYLINE** | **Synchronized Map Camera + Route Polyline for Inspectors:** On inspector open, camera frames user's station + route line. Active polyline illumination in agency color through fog (4px primary, 6px casing). Closure-based map commands from inspector to `ContentView` coordinator. Spec: design.md §10.5.2. | **M** | ✅ Done |
| **PA.6** | **WPA6-DESIGN-SYNC-AUDIT** | **G10 Update + design.md/ROADMAP Sync Audit:** Verify all guardrail cross-references are coherent after Screen 0–4 expansion. Cross-reference every design.md section against implementation status. Verify no dead references to removed inspector elements. | **XS** | ✅ Done |

### 🐛 Wave PB: Transit Data Fidelity & Scalable Destination Engine (Field Bugs 1–13)

> [!IMPORTANT]
> **Field Defect Remediation & Scalable Architecture.** Addresses 13 high-priority functional and visual defects discovered during hands-on Staten Island field testing. Rather than patching each bug with borough-specific hardcoding, Wave PB adopts the **industry-standard "Headsign-First" architecture** used by Citymapper, Transit App, and Apple Maps Transit — a 3-tier scalable terminal resolution pipeline that works automatically for every route in every city without writing a single line of city-specific Swift code.
>
> **Architecture Decision (Grilled & Approved):** All destination resolution flows through a strict 3-tier cascade:
> 1. **Tier 1 — GTFS-RT Live Stream:** Resolve terminal from the trip's last non-skipped `StopTimeUpdate.stop_id` → `transit.stops.stop_name`. Handles short-turns, construction detours, and emergency cut-backs with 100% agency truth in $\mathcal{O}(1)$.
> 2. **Tier 2 — City Pack SQLite:** Query `scheduled_hourly_patterns.headsign` compiled from GTFS `trips.trip_headsign`. Instant $<0.05\text{ms}$ lookup for all 500+ NYC and 200+ Boston routes.
> 3. **Tier 3 — Spatial Corridor Extrema:** Project `transit.stops` along the route's principal axis to find the two spatial endpoints. Zero fake data, 100% city agnostic.
>
> **Gated on:** Wave PA complete. Wave PB must ship before Wave Q (which depends on correct station sheet data).

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **PB.1** | **WPB1-SIR-IDENTITY** | **Staten Island Railway Canonical Identity & Realtime Feeds (Bugs 1, 2):** (1) Alias `"SI"` ↔ `"SIR"` in `TransitRouteData.lineInfo`, `TransitRealtimeService.SubwayFeed.feed(for:)`, `SubwayStationRegistry`, and `SpatialDatabaseManager.generateArrivals`. (2) Assign official MTA SIR Blue `#08179C` (replacing amber `#FFB300` default fallthrough). (3) Wire `"SI"` to the correct GTFS-RT feed endpoint (`nyct%2Fgtfs-si`), not the L train feed. (4) Direction labels: `"St George"` (Inbound) ↔ `"Tottenville"` (Outbound). (5) Terminal directional filtering: suppress impossible northbound departures at St George and impossible southbound departures at Tottenville. | **S** | ✅ Done |
| **PB.2** | **WPB2-STOP-NAME-SANITIZATION** | **Stop Name Sanitization & "BAY " Regex Disambiguation (Bug 3):** (1) In `SpatialDatabaseManager.isGenericStopName`, restrict `"BAY "` matching to standalone terminal bay patterns (`^BAY\s+\d+$` or `^BAY\s+[A-Z]$`) instead of prefix substring. Protect real street intersections containing `&`, `/`, and street suffixes (`St`, `Ave`, `Blvd`, `Pkwy`, `Rd`) from false-positive generic classification. (2) Clean dispatch noise tokens like `"Ramp B S51 & S81"` → `"St George Ferry Terminal"`. (3) Ensure `"Bay St & Victory Blvd"`, `"Bay Ridge Av"`, `"Bay Pkwy"`, and `"Baychester Av"` all pass through unmodified. | **XS** | ✅ Done |
| **PB.3** | **WPB3-REAL-STOP-LADDER** | **Scalable Bus Stop Ladder & Bounded Map Polyline (Bugs 4, 5, 6):** (1) In `SpatialDatabaseManager.fetchRouteStopLadder`, query real stops from `transit.stops WHERE routes LIKE '%,{routeId},%'` ordered by corridor spatial progression (latitude or stop_sequence). Eliminate the entire `generateFallbackStopLadder` function and all hardcoded mock stops (`"Midtown Crossing"`, `"Central Square"`, subway `4`/`5` transfer badges, `(40.7580, -73.9855)` Manhattan coordinates). (2) Anchor fallback inspector coordinates to the **tapped stop's actual coordinate**, eliminating the 15-mile camera displacement from Staten Island to Midtown Manhattan. (3) Construct inspector polyline from the real stop coordinate chain (or pre-packaged route shape from `transit-lines.geojson`). (4) **Camera Safety Invariant:** Inspector camera framing is strictly bounded to the real stop ladder's coordinate bounding box — it is mathematically impossible for the camera to fly to another borough. | **M** | ✅ Done |
| **PB.4** | **WPB4-UI-LAYOUT-INTEGRITY** | **UI Layout Integrity, Badge Flow & Matrix Deduplication (Bugs 7, 8, 9, 10, 11, 12):** (1) Add `.fixedSize(horizontal: true, vertical: false)` and `.lineLimit(1)` to `TransitRouteBadge` (`.regular` case) to prevent vertical text squishing of bus route labels. (2) Deduplicate `routeIds` array in `StopDetails` (`Set`-based dedup in `SpatialDatabaseManager.fetchStopDetails`). (3) Remove redundant text label from timetable filter pills when `TransitRouteBadge(.filter)` already renders the route name (eliminating `[S51] S51` → `[S51]`). (4) Clamp timetable `NEXT` badge to a **single** earliest departure anchor per direction (break ties by `routeId` lexicographic order). (5) Anchor `NearbyBusesCapsule` expanded card to `.leading` edge alignment (replace `VStack(alignment: .trailing)` with `.leading`). (6) Add `MLNShapeSourceOption.clustered: true` with `clusterRadius: 30` to `nearby-bus-stops-layer` source to eliminate overlapping blue circle "grapes" at transit hubs (St George, Port Authority). | **M** | ✅ Done |
| **PB.5** | **WPB5-HEADSIGN-ENGINE** | **Scalable Headsign-First Destination Engine & Multi-City Direction Labels (Bug 13):** (1) **Observer Compile-Time:** Add `route_directions` table to `transit.sqlite` during city pack compilation: `(route_id TEXT, direction_id INT, headsign TEXT, terminal_stop_id TEXT)` derived from the statistical mode of `trips.trip_headsign` in GTFS static. ~25 KB per city, zero client hardcoding for 500+ NYC routes. (2) **Client 3-Tier Cascade:** In `TransitRealtimeService.resolveDestination`, implement: Tier 1 — inspect GTFS-RT `TripUpdate.stop_time_update` in reverse to find the last non-skipped `stop_id`, resolve via `transit.stops.stop_name`. Tier 2 — query `route_directions.headsign` from SQLite. Tier 3 — spatial corridor extrema fallback projecting `transit.stops` along the route's principal axis. (3) **Direction Label Normalization:** Replace Manhattan-centric `"Uptown & Northbound"` / `"Downtown & Southbound"` on surface buses with clean headsign-first `"To [Terminal]"` labels (e.g. `"To St George Ferry"`, `"To Midland Beach"`). Restrict `"Uptown"` / `"Downtown"` jargon exclusively to Manhattan heavy-rail subway corridors. (4) **Corridor Note Deduplication:** Suppress `corridorNote` in `TransitRevealSheet` when the section note duplicates the arrival row destination verbatim. | **M** | ✅ Done |

### 🧑‍🔧 Wave PC: Commuter Ergonomics & First-Click Usefulness Overhaul

> [!IMPORTANT]
> **Commuter Trust & Ergonomic Remediation.** Addresses 12 high-priority usability and layout bugs discovered during systematic first-click cognitive auditing across Screens 1–3. These defects directly degrade daily commuter trust — dead past space on the L train inspector, 56pt timetable collisions at 42 St–PABT, developer jargon in hero headers, and nested sheet stacking. Wave PC formalizes the **First-Click Usefulness Audit** protocol as an ongoing UX invariant.
>
> **Gated on:** Wave PB complete. Wave PC must ship before Wave Q (which depends on a clean, commuter-grade station sheet).

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **PC.0** | **WPC0-FIRST-CLICK-AUDIT** | **5-Screen First-Click Usefulness Audit & Invariant Checklist:** Formalize the First-Click Cognitive Audit across Screens 0–4 into [`docs/design.md`](file:///Volumes/T7ssd/derivee/docs/design.md) and [`DERIVEE_INVARIANTS.md`](file:///Volumes/T7ssd/derivee/.agents/skills/ux-audit/DERIVEE_INVARIANTS.md). Every interactive entry point must answer the user's primary commuter question above the fold within 0.0s of opening: (1) Zero dead past space without an active anchor, (2) Zero raw database keys or telemetry acronyms, (3) Zero duplicate status pills, (4) Zero unclipped height collisions, (5) Zero nested sheet stacking, and (6) Thumb zone reserved for high-frequency insights. | **S** | ✅ Done |
| **PC.1** | **WPC1-INSPECTOR-ANCHORING** | **Run Inspector Active Station Anchoring, Live Map Vehicle Tracking & Passed Stops Accordion (Bug 1.1, 1.4):** (1) Auto-anchor stop progression ladder to active station (`isCurrent`). (2) Collapse preceding completed stations into disclosure accordion (`X stops passed ▾`, default collapsed when passed count $\ge 3$). (3) **"Where Is My Train" Live Map Tracking:** Outline full route line through fog and render real-time vehicle puck (puck in route color, pulsing halo, and bearing/glyph) on MapLibre via `ephemeral-vehicle-source`. Resolve coordinates in `TransitRealtimeService` (GPS for buses, station snapping/interpolation for subways). (4) Replace verbose crowding title with glanceable tri-tier status badge (`🟢 Light / 🟡 Moderate / 🟠 Crowded`). | **M** | ✅ Done |
| **PC.2** | **WPC2-CLEAN-TELEMETRY-HERO** | **Commuter-Centric Hero Header & Telemetry De-Jargonization (Bug 1.2, 1.3):** (1) Purge raw database `trip_id` hash strings (`TRIP 091850_L..N`) and `REALTIME AVL` acronyms from both inspectors. Replace with actionable commuter context: vehicle proximity (*"2 stops away • Approaching Lorimer St"*) and follow-on departure imminence (*"Next train in 4 min"*). (2) Eliminate duplicate stacked status: when `arrival.minutes == 0` and `distanceDescription == "Boarding"`, unify into a single consolidated route token `[ L ] 8 Av • Boarding (Track 1)`. | **S** | ✅ Done |
| **PC.3** | **WPC3-SINGLE-SHEET-TRANSITION** | **Single-Sheet Transition Hierarchy (Bug 1.5):** Refactor `TransitRevealSheet` inspection flow to eliminate nested modal sheet-on-sheet presentation (`.sheet(item: $inspectingArrival)`). Transition the parent station sheet into the inspector view in-place (or push onto a unified navigation stack with smooth slide transition and back chevron), preserving fluid interactive drag dismissal and eliminating the squished background parent card. | **M** | ✅ Done |
| **PC.4** | **WPC4-TIMETABLE-MATRIX-FIX** | **Departure Matrix Collision Fix, Single-Route Scope & Wall-Clock Scroll (Bug 2.1–2.4):** (1) Remove hardcoded `.frame(height: 56.0)` from `HourRowView` to eliminate multi-line wrap overflow and collision with subsequent hours. (2) Replace repeated hour prefixes (`14:21` inside `14:00` row) with clean, reference-grade `:21` minute capsules. (3) Disambiguate express branches with destination badges instead of generic `EXP`. (4) Default `selectedRouteFilter` to the first active route (e.g. `[ A ]`) with horizontal tabs for co-located lines (`[ C ]`, `[ E ]`), eliminating the chaotic multi-trunk `"All Routes"` stream. (5) Fix the dead-end `guard !isInitialized` in `.task` so `.scrollPosition(id:)` reliably anchors to the active wall-clock hour upon first render. | **M** | ✅ Done |
| **PC.5** | **WPC5-VIEWPORT-OPTIMIZATION** | **Station Chrome & Exploration Journal Thumb Zone Polish (Bugs 3.1, 3.2):** (1) Tighten vertical padding in `TransitRevealSheet` header, service alerts, and floorplan stepper so at least **3 to 4 live arrival rows** are immediately visible above the fold at `.medium` detent. (2) Relocate the persistent, full-width `"Upload Previous Workouts"` GPX button in `StatsView` into `SettingsView` or a navigation toolbar menu, restoring the bottom 100pt thumb zone to exploration progress and journal insights. | **S** | ✅ Done |

### 🧭 Wave PD: Commuter Map Fidelity & Transit Coherence Overhaul

> [!IMPORTANT]
> **Commuter Map & Transit Data Coherence Gate:** Resolves 9 high-priority field defects across multi-station complex clustering (Times Sq, Fulton St, Union Sq), canonical agency route ordering (A C E), live train dynamic framing, interactive 3-detent sheet docking, strictly local de-expression (L train), pedestrian recentering zoom, surface bus corridor progression, regional station bullet decluttering, intermediate approaching stop ETAs, viewport anchoring for distant consists, stop name clipping, adaptive transfer pill expansion, and complex transfer route disambiguation.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **PD.1** | **WPD1-STATION-COMPLEX-UNIFICATION** | **Multi-Modal Station Complex Unification & Canonical Ordering:** (1) In `MapView.loadPOIs` and `SpatialDatabaseManager.fetchStopDetails`, join `transit.stop_resolution` and `transit.complexes` to unify member parent stations (e.g. Times Sq `127, 725, 902, A27, R16`, Fulton St, Union Sq) into single complex stations. (2) Display unified title `Times Sq-42 St / 42 St-PABT`. (3) Enforce canonical MTA transit agency route ordering (`1, 2, 3, 4, 5, 6, 6X, 7, 7X, A, C, E, B, D, F, FX, M, G, J, Z, L, N, Q, R, W, S, SIR`) across all route badges, transfer lists, and composite bullets. | **M** | ✅ Done |
| **PD.2** | **WPD2-INTERACTIVE-INSPECTION-DOCK** | **Interactive 3-Detent Persistent Dock & Return Navigation:** (1) Eliminate `dismiss()` in `GuidewayRunInspector` and `SurfaceRunInspector.synchronizeWithMap()`. (2) Add `.fraction(0.12)` (~90pt) bottom peek detent to `TransitRevealSheet` when inspecting an arrival, rendering a compact interactive pill `[Route Badge] [Destination] • [ETA] [^ Tap to Expand]`. (3) Keep map 100% interactive for free panning/zooming during inspection, with 1-tap re-expansion to `.medium` / `.large`. (4) Tapping `(X)` exits inspection back to station departures. | **S** | ✅ Done |
| **PD.3** | **WPD3-APPROACHING-TRAIN-FRAMING** | **Approaching Train Progression Segment & Bounded Vehicle Framing:** (1) Stop ladder starts at oncoming train's current stop (`Graham Av • TRAIN HERE`) with live pulsing vehicle marker, renders intermediate approaching stops, highlights `YOU ARE HERE` at commuter station, and auto-scrolls so both vehicle and user station are visible above the fold. Truncate/collapse stops prior to live train into `[v] X earlier stops`. (2) In `MapView.frameRouteAndStation`, calculate tight bounding box between user station and live vehicle, clamping zoom to $z \in [14.5, 15.5]$ with asymmetric bottom sheet padding. | **M** | ✅ Done |
| **PD.4** | **WPD4-LOCAL-DEEXPRESS-RECENTER-ZOOM** | **Strictly Local Route De-Expression & Pedestrian Recenter Zoom:** (1) Register `strictlyLocalRoutes` (`L`, `G`, `1`, `C`, `M`, `R`, `W`, `S`, `SIR`) in `TransitRealtimeService.resolveDestination` and `SubwayStationRegistry`, forcing `isExpress = false` and suppressing `" EXP"` when westbound trains report Track 2. (2) In `MapView.swift`, set camera zoom explicitly to $z = 16.0$ on `recenterTrigger` activation, bringing commuters into immediate pedestrian street scale. | **XS** | ✅ Done |
| **PD.5** | **WPD5-SURFACE-CORRIDOR-PROGRESSION** | **Surface Bus Corridor Progression & Map Sawtooth Elimination (Field Defect):** (1) In `SpatialDatabaseManager.fetchRouteStopLadder`, replace 1D latitude sort with Corridor Progression Engine: parse thoroughfares and cross-streets, maintain thoroughfare persistence (Kent Ave NB vs. Wythe Ave SB on route B32; 11th St NB vs. 21st St/Jackson Ave SB in Queens), and enforce cross-street uniqueness. (2) Eliminate zig-zag stop ladder and sawtooth map polyline (Field proof: B32 alternating Kent Av / Wythe Av ladder and diagonal map zig-zags). | **M** | ✅ Done |
| **PD.6** | **WPD6-STATION-BULLET-ZOOM-HARDENING** | **Station Macro Bullet Zoom Decluttering:** (1) In `StationTransitVisualizationManager`, add lower zoom interpolation stops to `bulletRadiusExpression` ($0.0\text{pt}$ at $z \le 11.5$) and `bulletOpacityExpression` ($0.0$ at $z \le 11.5$). (2) Set `bulletsLayer.minimumZoomLevel = 11.5` in `MapView.swift` to eliminate GPU draw calls and declutter Lower Manhattan and bridge corridors at regional view. | **S** | ✅ Done |
| **PD.7** | **WPD7-APPROACHING-ETAS-VIEWPORT-ANCHOR** | **Approaching Stops Progressive ETAs & Distant Consist Viewport Anchoring (Field Defect):** (1) **Progressive Approaching ETAs:** In `TransitRealtimeService.annotateLadderWithVehicle`, calculate countdown ETAs for all intermediate stops between live vehicle (`vehicleIdx`) and commuter station (`currentIdx`): $\text{eta}_i = \max(1, \text{currentArrivalMinutes} - (\text{currentIdx} - i) \times 2)$. Eliminates blank time labels on approaching stops (Field proof: missing Lorimer/Flushing/Myrtle ETAs on M train; blank Wilson through Morgan on L train; missing Wythe & Metropolitan ETA on B32). (2) **Distant Consist Viewport Anchoring:** When oncoming vehicle is $>4$ stops away from commuter station, prioritize scroll anchoring to `ACTIVE_STATION` (`anchor: .center`) instead of forcing `VEHICLE_STOP` to `.top`, or collapse long intermediate approaching spans into an expandable accordion (`[v] X approaching stops`), preventing the commuter's station (`YOU ARE HERE`) from scrolling off-screen below the fold (Field proof: Bushwick Av-Aberdeen vehicle pushing Bedford Av completely off-screen). | **S** | ✅ Done |
| **PD.8** | **WPD8-STOP-NAME-CLIPPING-PILL-EXPANSION** | **Station Name Truncation Elimination, Express Diamond Badges & Adaptive Route Pill Expansion (Field Defect):** (1) **Stop Name Clipping Elimination:** In `GuidewayRunInspector` and `SurfaceRunInspector`, allow stop names to wrap cleanly (`.lineLimit(2)`) with `.minimumScaleFactor(0.85)` and optimized badge padding so compound station names (`Bushwick Av-Aberdee...`, `Kent Av & Metropolitan...`) never truncate to ellipses when flanked by `• TRAIN HERE` / `YOU ARE HERE` badges and right-aligned ETAs. (2) **NYC MTA Express Diamond Badges:** Introduce vector-crisp `DiamondShape`. Update `TransitRouteData` and `TransitRouteBadge` to recognize express subway variants (`6X`, `7X`, `FX`) and render canonical MTA diamond bullets `<6>`, `<7>`, `<F>` with centered root glyphs across all badge sizes (`large`, `regular`, `compact`, `filter`, `tiny`) and in transfer lists. (3) **Adaptive Badge Pill Layout:** Replace hardcoded `frame(width: 14, height: 14).clipShape(Circle())` with adaptive badge layout (`minWidth: 14`, horizontal padding for multi-char identifiers, `.clipShape(Capsule())`), eliminating `...` truncation for bus (`B62`, `B32`) and rail lines (`SIR`, `PATH`). (4) **Branch vs. Express Architecture Policy:** Enforce circular bullets `(A)` for physical line branches (e.g. A to Lefferts vs Far Rockaway vs Rockaway Park), disambiguating forks strictly via destination badges and terminus progression rather than pseudo-express diamonds. | **S** | ✅ Done |
| **PD.9** | **WPD9-TRANSFER-COMPLEX-GLYPH-DISAMBIGUATION** | **Transfer Route Disambiguation Glyphs & Multi-Complex Resolution in Stop Ladders (Field Defect):** (1) **Transfer Route Disambiguation:** Prefix transfer route badges with a subtle `⇄` transfer glyph or distinct secondary styling (e.g., `⇄ [J] [Z]`) instead of raw standalone circular bullets directly under the station name, eliminating commuter confusion between connecting lines and the inspected train's route identity (Field proof: M train ladder showing standalone J/Z bullets under Hewes, Lorimer, Flushing, Myrtle with missing M bullet). (2) **Complex Station Transfer Resolution:** In `SpatialDatabaseManager.fetchRouteStopLadder`, join `transit.stop_resolution` and `transit.complexes` to aggregate transfer routes from all member child stops in a complex. Ensures major multi-line hubs display complete transfer options (Field proof: Myrtle-Wyckoff missing L transfer on M train; Lorimer missing G transfer and Union Sq missing 4/5/6/N/Q/R/W transfers on L train). | **S** | ✅ Done |

### 🗺️ Wave PE: Commuter Map Viewport & Persistent Inspection Overhaul

> [!IMPORTANT]
> **Commuter Map Viewport, Cartography & Field Integrity Gate:** Resolves critical field defects in transit route inspection, map visibility, commuter location agency, multi-line complex direction clustering, and basemap contrast across six structured tracks:
> - **Track 1 (Map Viewport & Inspection Engine):** Decouples route and vehicle telemetry from inspector sheet detent transitions (eliminating `.onDisappear` telemetry wipes in peek dock), implements detent-aware dynamic camera framing with relaxed zoom constraints ($z \in [13.0, 15.5]$), and introduces adaptive transit fog lighting ($0.85 \to 0.40$).
> - **Track 2 (Station Departures & Timetable Intelligence):** Unifies bus stop arrivals into a single chronological stream with multi-route filter chips, unlocks tap-to-inspect run inspection across all 24-hour timetable pills with a Dual-Mode inspector and strict terminus delay gating, eliminates synthetic timetable placeholders in favor of honest unavailable states, and suppresses phantom reverse directions at rail termini and one-way bus curbs (PE.4, PE.5, PE.13).
> - **Track 3 (Screen 1 Location Agency Controls):** Equips the primary map canvas with direct Ambient Drift and Dynamic Island quick controls, giving commuters 1-tap agency over background GPS tracking and Live Activity visibility.
> - **Track 4 (Complex Direction Canonicalization & Coherence):** Eliminates fragmented directional headers at complex multi-line hubs (e.g., 34 St-Herald Sq, Boston Park St) via an agency-agnostic 2-tier direction classifier, mapping trains to unified cardinal vectors while keeping branch terminals in row subtitles.
> - **Track 5 (Universal Cartography Contrast & Polyline Stitching):** Universal relative luminance contrast engine dynamically adjusting casing for low-contrast lines (e.g., L train `#A7A9AC`, Boston Silver Line `#7C878E`) against `#F9F9F6` basemaps, adjusts sub-fog thoroughfare opacity, and fixes MultiLineString coordinate chain assembly.
> - **Track 6 (Single-Sheet Presentation Architecture & Seam Elimination):** Refactors `ContentView` sheet presentation to a mutually exclusive state machine, synchronizes glass presentation backgrounds with detent animations, eliminates grabber header collisions, and expands the local ergonomic bouncer with Invariants FC-11 to FC-14.
>
> **Execution Dependency Order:** Track 1 (PE.1 $\to$ PE.2 $\to$ PE.3) establishes the persistent viewport and basemap lighting. Track 2 (PE.4 $\to$ PE.5, PE.13) cleans bus arrival feeds, connects timetable pills to the now-stable inspector, and eliminates synthetic timetable placeholders / terminus direction leakage. Track 3 (PE.6) implements the standalone Screen 1 location controls. Track 4–6 (PE.7 $\to$ PE.12) resolve field defects in cartography, complex direction cohesion, and sheet presentation hierarchy.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **PE.1** | **WPE1-PERSISTENT-INSPECTION-LIFECYCLE** | **Persistent Route & Vehicle Telemetry Across Sheet Detents:** (1) Decouple route inspection lifecycle from the view hierarchy of `GuidewayRunInspector` and `SurfaceRunInspector`, eliminating premature `onClearRouteInspection?()` invocations triggered by `.onDisappear` when lowering the drawer to `.fraction(0.12)` (`inspectionPeekDetent`). (2) Ensure route polyline, casing, vehicle halo, and train puck remain pinned to the map while interacting with `compactInspectionDockPill`. (3) Restrict inspection teardown strictly to explicit exit actions (tapping `(X)` close button or back chevron). | **S** | ✅ Done |
| **PE.2** | **WPE2-DETENT-CAMERA-FRAMING** | **Detent-Aware Dynamic Camera Viewport Framing:** (1) In `MapView.swift:frameRouteAndStation`, adapt `bottomPadding` dynamically based on active sheet detent (~110pt at peek dock vs ~360pt at `.medium`), preventing the active station and route from being buried under the bottom sheet. (2) Relax rigid minimum zoom clamping ($z \ge 14.5$), allowing the camera to smoothly frame both the oncoming consist and user station across longer approach spans ($z \in [13.0, 15.5]$). (3) Maintain center coordinate reprojection so track curvature and vehicle tokens sit centered in the unoccluded viewport. | **M** | ✅ Done |
| **PE.3** | **WPE3-ADAPTIVE-TRANSIT-FOG** | **Adaptive Transit Fog Illumination & Basemap Contrast:** (1) Automatically transition `fogOpacity` down from $0.85$ to `MapCustomizationDefaults.transitFogOpacity` ($0.40$) when inspecting a station or active route, revealing street grids, parks, and water in pure Light Mode parchment (`#F9F9F6`) without dark slate murkiness. (2) In `MetalFogStyleLayer`, ensure inspected corridor track geometry cuts cleanly through the fog layer with clear visual priority. (3) Smoothly restore ambient fog opacity upon returning to standard exploration mode. | **S** | ✅ Done |
| **PE.4** | **WPE4-BUS-STOP-UNIFIED-CHRONOLOGICAL-DEPARTURES** | **Bus Stop Unified Chronological Departures & Multi-Route Filter Chips (Field Defect):** (1) **Unified Chronological Stream for Bus Stops:** In `TransitRevealSheet.swift`, when `stopDetails?.modalClass == .bus`, eliminate subway-style bi-directional grouping (`DirectionalArrivalGroup`) and redundant pseudo-direction headers (e.g., `TO 1 AV & 24 ST` directly above `1 Av & 24 St`). Render upcoming bus arrivals as a single, unified chronological stream sorted strictly by ETA, displaying terminal branch variants and short-turns (e.g., M9 to 24th St vs M9 to E 20th St Loop) cleanly within the arrival rows. (2) **Decouple Direction from Destination:** In `TransitRealtimeService.resolveDirection` and `SpatialDatabaseManager`, eliminate fallback string interpolation `"To \(destination)"` into `arr.direction`. Map bus directions to true macro corridor vectors (`Northbound`, `Southbound`, `Eastbound`, `Westbound`, `Inbound`, `Outbound`, or borough targets like `Queens-bound`) derived from GTFS `direction_id` and geometry. (3) **Multi-Route Bus Hub Filter Chips:** When a bus stop serves multiple distinct lines (`routeIds.count > 1`, e.g. `BM1, BM2, BM3, BM4, M22` or `M1, M2, M3, M4`), introduce horizontal route filter chips (`[All (N)] [Route1] [Route2] ...`) with haptic feedback (`UIImpactFeedbackGenerator(style: .light)`), allowing commuters at multi-line transfer curbs to either scan all oncoming buses or isolate their specific route. (4) **Single-Direction Timetable Consistency:** In the `Full Timetable` tab for bus stops, adapt direction toggles to reflect the stop's actual serving direction or route destination instead of hardcoded bi-directional `Uptown / Downtown` selectors when only a single direction is physically served. | **S** | ✅ Done |
| **PE.5** | **WPE5-TIMETABLE-RUN-INSPECTION** | **Full Timetable Run Inspection & Terminus-Gated Delay Architecture:** (1) Tap-to-inspect interaction on all past, live, and future departure pills in `DepartureMatrixView` via `onInspectDeparture` routing directly into `TransitRevealSheet`'s single-sheet inspector stack. (2) Dual-Mode Inspector (`Live Run` vs `Scheduled Run` / `Historical Replay`): render static timetable stop ladder with downstream wall-clock times from GTFS `stop_times` when no active consist is present; automatically promote to live tracking with vehicle puck when matched to an active consist. (3) Strict Terminus Delay Gating: suppress numerical delays (`+Xm`) on departure pills and stop ladders for unassigned trips (`is_assigned: false`) or consists dwelling at origin terminals (`status == STOPPED_AT`, `stop_sequence <= 1`); unlock live delays only when `status == IN_TRANSIT_TO` or vehicle passes sequence 1; flag `HOLDING_STATION` / `"Held at Station"` at the origin terminus if delayed >120s. (4) Qualitative Disruption Badging: when GTFS-RT alerts or announcements report corridor delays, display the active disruption banner and tag run as `"Delays Reported"` without synthesizing fake minute delays on un-dispatched trips. (5) Historical Replay: for past departures, surface recorded `stop_events` outcomes (`"Departed +2m late"`) with `"Passed"` stop ladders and static route framing. | **M** | ✅ Done |
| **PE.6** | **WPE6-AMBIENT-DRIFT-QUICK-CONTROLS** | **Main Screen Ambient Drift & Dynamic Island Quick Controls (Direct Location Agency):** (1) **Map Canvas Control Button:** On Screen 1 (`ContentView.swift`), introduce a native frosted-glass Quick Controls button (`AmbientDriftFAB` / `DriftStatusPill`, styled with `.ultraThinMaterial`, `UIImpactFeedbackGenerator(style: .light)`). Includes a live status indicator (pulsing Electric Amber `#FFB300` beacon dot when active, subtle neutral dot when paused) providing 0.0s ambient state awareness without obscuring the map. (2) **Crystalline Quick-Control Modal / Popover:** Tapping the control button opens an ergonomic, lightweight modal sheet or anchored popover (`AmbientDriftControlCard` / `.presentationDetents([.height(280)])` / native popover with `.presentationBackground(.ultraThinMaterial)` and `.presentationDragIndicator(.visible)`). Adheres strictly to iOS 17+ human interface guidelines and zero-nested-sheet invariants (FC-5). (3) **Direct Interactive Switches & Telemetry Glance:** Direct switch for **Ambient Drift** (background location tracking & hex discovery via `trackingEngine.isTracking` / `isTrackingEnabled` with operational subtitle: `"Active • Background GPS"` vs `"Paused • Battery Preserved"`), eliminating the friction of navigating 3 layers into Settings; direct switch for **Dynamic Island & Lock Screen Glance** (`trackingEngine.isLiveActivityEnabled`) with immediate lifecycle synchronization (`startLiveActivity()` / `endLiveActivity()`); glanceable CoreLocation authorization status badge (`Always`, `While Using`, `Denied`) with inline one-tap permission escalation prompt if degraded. (4) **Ergonomic Safety & Dismissal:** Tap-outside or drag gesture dismisses cleanly with spring physics; fully decoupled from Screen 2 transit reveal sheets and Screen 4 navigation workflows. | **S** | ✅ Done |
| **PE.7** | **WPE7-COMPLEX-DIRECTION-CANONICALIZATION** | **Universal Multi-Modal Direction Clustering & Complex Coherence (Field Defect):** (1) Replace fragmented string hardcoding in `TransitRealtimeService.resolveDirection` with an agency-agnostic 2-tier classification: Tier 1 resolves canonical corridor vector (`.inbound` / `.outbound`, `.northbound` / `.southbound`, `.eastbound` / `.westbound`) from GTFS `direction_id` and spatial stop progression; Tier 2 resolves terminal branch / short-turn qualifiers from `route_directions.headsign` or last non-skipped `stop_name`. (2) In `TransitRevealSheet.groupedArrivals`, cluster arrivals by canonical corridor vector instead of raw literal string equality, unifying all co-located lines at complex hubs (e.g., 34 St-Herald Sq B/D/F/M and N/Q/R/W under a single `UPTOWN & QUEENS / THE BRONX` section; Boston MBTA Park St Green Line branches under unified `INBOUND` / `OUTBOUND`). (3) Terminal qualifier disambiguation: display branch terminals (e.g., `W to Whitehall St`, `Green Line C to Gov Center`) within row subtitles without misclassifying their parent cardinal direction (eliminating labeling Lower Manhattan as Brooklyn). | **S** | ✅ Done |
| **PE.8** | **WPE8-LUMINANCE-CONTRAST-CARTOGRAPHY** | **Universal Basemap Contrast Engine & MultiLineString Polyline Stitching (Field Defect):** (1) **Dynamic Luminance Contrast Engine:** In `TransitCartographyLoader` and `TransitRouteData`, implement WCAG 2.1 relative luminance calculation ($L = 0.2126R + 0.7152G + 0.0722B$). For routes where contrast against the basemap background (`#F9F9F6` Day, `#1C1C1E` Night) falls below $3.0:1$ (e.g. NYC L train `#A7A9AC`, Boston Silver Line `#7C878E`, London Circle Line `#FFD300`), dynamically inject an adaptive dark charcoal casing (`#2C2C2E` / `#3A3A3C`, 1.5pt) or contrast-boosted stroke, guaranteeing instant legibility on both day and night basemaps. (2) **Sub-Fog Thoroughfare Legibility:** In `MapView.swift`, adjust subway casing and line opacity/blending under the fog mask so thoroughfares remain clearly discernible as orienting guidelines without being blackened out by the 85% fog layer. (3) **MultiLineString Topological Chain Assembly:** Replace naive `max(by: count)` flattening in `TransitRouteData.loadCoordinatesFromGeoJSON` with topological station-connected coordinate chain assembly, eliminating straight diagonal jump shortcuts across city grids. (4) **Squiggle Elimination:** Suppress degenerate disjoint polygon rings (<3 vertices or area < 10m²) in `FogPolygonMath` that produce stray floating amber loops. | **M** | ✅ Done |
| **PE.9** | **WPE9-SINGLE-SHEET-ARCHITECTURE** | **Single-Sheet Mutually Exclusive Presentation Architecture & Seam Elimination (Field Defect):** (1) In `ContentView.swift`, refactor sheet presentation to a unified `ActiveSheet` enum state machine (`.transit(stopId:)`, `.driftControls`, `.stats`, `.search`, `.cityPrompt`), eliminating concurrent `.sheet` modifier bindings on the same view hierarchy. (2) Enforce strict mutual exclusivity: opening `AmbientDriftControlCard` or `StatsView` cleanly dismisses or pauses `TransitRevealSheet`, preventing background sheet retention. (3) **Midpoint Horizontal Line Elimination:** Synchronize `.presentationBackground` (`TransitSheetGlassBackground`) with active detents so the sheet container and specular gradient border animate down to `.fraction(0.12)` (`inspectionPeekDetent`) in lockstep, eliminating the frozen 50% medium detent horizontal line and lower-half map blur. | **M** | ✅ Done |
| **PE.10** | **WPE10-SHEET-CHROME-LAYOUT-INTEGRITY** | **Universal Sheet Chrome Clearance & Non-Truncating Telemetry Banners (Field Defect):** (1) In `AmbientDriftControlCard.swift`, increase header top padding from `2pt` to `24pt`, providing proper clearance below the system `.presentationDragIndicator(.visible)` and eliminating grabber-title collision. (2) In `AmbientDriftControlCard.escalationBanner`, apply `.fixedSize(horizontal: false, vertical: true)` with `.layoutPriority(1)` to `Text(escalationMessage)` and optimize horizontal spacing so warning messages (`"Background discovery requires 'Always' authorization"`) wrap cleanly to 2 lines without truncating to `'Al...`. | **XS** | ✅ Done |
| **PE.11** | **WPE11-LADDER-CONTINUITY-POLYLINE-SYNC** | **Stop Ladder Continuity, Preceding Stops Accordion & Route Polyline Illumination (Field Defect):** (1) In `SpatialDatabaseManager.fetchRouteStopLadder`, query the complete trip sequence from `transit.stop_times`, ensuring all origin and intermediate preceding stops are loaded. (2) In `GuidewayRunInspector.swift`, auto-collapse preceding stops into the `[v] X stops passed` accordion when passed count $\ge 3$, anchored cleanly to the commuter's station (`YOU ARE HERE`). (3) Draw a continuous, solid vertical route progression stroke connecting stop dots without dash gaps or segment breaks. (4) Ensure `onInspectRoute` reliably dispatches the full route coordinate chain to `MapView.updateRouteInspection`, illuminating the active corridor through the fog. | **S** | ✅ Done |
| **PE.12** | **WPE12-LOCAL-BOUNCER-MULTI-CITY-INVARIANTS** | **Local Ergonomic Bouncer Multi-City Expansion (Invariants FC-11 to FC-14):** (1) Expand `.githooks/pre-commit` and `scripts/verify-ux.sh` (`CommuterErgonomicsTests.swift`) with 4 new shift-left invariants: FC-11 (Header Grabber Clearance: $\ge 20\text{pt}$ top padding on sheets with visible drag indicators), FC-12 (No Truncated Status Banners: no ellipsis clipping in permission / alert banners), FC-13 (Complex Direction Coherence: no duplicate or fractured direction buckets sharing cardinal prefixes), FC-14 (Basemap Contrast Minimum: every route in `transit-lines.geojson` passes WCAG AA contrast against basemap). | **S** | ✅ Done |
| **PE.13** | **WPE13-HONEST-TIMETABLES-TERMINUS-CURB-GATING** | **Honest Timetable State, Placeholder Schedule Elimination & Single-Direction Terminus / Curb Gating (Field Defect):** (1) **Cut Synthetic Timetable Placeholders:** In `SpatialDatabaseManager.swift:fetchTimetableResult`, completely eliminate synthetic schedule generation (`generateFallbackTimetable` / `generateMultiRouteFallbackTimetable`) that invents fake departure minutes (:14, :22, :30...) for stops without static GTFS schedule rows in `scheduled_hourly_patterns` (e.g. bus stops or uncompacted lines). Instead, return empty records with `isScheduleAvailable = false`. In `DepartureMatrixView.swift`, replace the 24 blank rows of `"No scheduled service"` with a clean, honest empty state: `"Timetable Not Available • Scheduled timetable is not available for this stop. Live arrival tracking is active."` (or `"No scheduled departures found for this direction"`). (2) **Terminal Direction Filtering & Arrival Exclusion:** Eliminate phantom reverse directions and arrival leakage at terminal stations (e.g. Pelham Bay Park `601`, South Ferry `142`, Flushing-Main St `701`, 8 Av `L01`, Canarsie `L29`, Tottenville `S09`, St George `S31`). In `fetchAvailableDirections`, filter out directions where all patterns terminate at the station (`headsign == stop_name` or matching station complex). In `fetchTimetableResult`, strictly exclude terminating arrival patterns from departing pills (`p.headsign != s.stop_name`), eliminating phantom arrivals rendering as departures (e.g. 108 uptown arrivals at Pelham Bay Park). (3) **Single-Direction Terminus & One-Way Curb Mode-Adaptive UI:** In `DepartureMatrixView.swift`, generalize single-direction handling beyond buses to all modes (`availableDirections.count == 1`). Replace the 2-segment picker `[ Uptown & Bronx | Downtown & Brooklyn ]` with a dedicated single-direction header pill: `"Serving \(direction) • Terminal Station"` for rail/subway termini, and `"Serving \(direction) • One-Way Curb"` for single-curb bus stops. Automatically lock `selectedDirection` to the single available direction. (4) **1-Direction Bus Stop Detection:** Expand one-way corridor and avenue detection in `generateFallbackAvailableDirections` (1st, 2nd, 3rd, 5th, Madison, 6th, 7th, 9th, 10th, Lexington Aves; directional tokens `NB`, `SB`, `EB`, `WB`), and correlate with active live arrivals so 1-direction bus stops reliably present as single-direction curbs rather than bi-directional pickers. | **S** | ✅ Done |

#### 🚨 Pre-Wave T Field Test Audit & Remediation Ledger (`test_run_1.MP4`)

> [!CAUTION]
> **Field Test Gate Prior to Wave T:** The untethered physical iPhone field test recording (`recordings/test_run_1.MP4`, 02:23 duration) audited against the 7 Field Stations, First-Click Invariants (FC-1 to FC-14), and grilled via Red-Team Architectural Review (`red_team_critique.md`) revealed 7 critical functional regressions, concurrency traps, and UI defects.
>
> All 7 tasks (**Pre-T.1 through Pre-T.7**) are hard blockers that **must be remediated, unit-tested via TDD, and verified via `./scripts/verify-ux.sh` before commencing Wave T (Navigation Hardening)**.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **Pre-T.1** | **WPT1-LENS-COLLISION** | **Floating Lens Collisions & Hit-Testing Hierarchy (Station 7 / FC-5):** (1) Auto-collapse `isNearbyBusesExpanded = false` in `ContentView.swift` whenever `selectedTransitStop != nil` or `activeSheet != nil`. (2) Add `.allowsHitTesting(activeSheet == nil)` alongside `.opacity` on `NearbyBusesCapsule` and `RecenterFAB` to eliminate invisible tap-interception traps over the lower sheet. | **XS** | ✅ Done |
| **Pre-T.2** | **WPT2-DIRECTION-PRECEDENCE** | **3-Tier Direction Classification & Canonical Direction Storage (Station 4 / FC-13, PE.7):** (1) Enforce strict 3-tier direction hierarchy in `TransitRealtimeService.classifyDirection`: **Tier 1 — Explicit GTFS-RT `directionId`** (when `hasDirectionID == true`), providing immunity against reverse single-tracking (L, 7, G lines) and construction reroutes where platform suffixes flip; **Tier 2 — Headsign Terminal Matching** against known terminal names; **Tier 3 — Platform Suffix** (`N`/`S`/`E`/`W`) as fallback only. (2) Apply identical 3-tier precedence to the SIR early-return branch (`S31N`/`S31S`). (3) Store canonical `directionId: Int` (0 or 1) as a first-class field on `ArrivalInfo` at ingest time, deprecating brittle string parsing in `SpatialDatabaseManager.resolvedDirectionId`. | **M** | ✅ Done |
| **Pre-T.3** | **WPT3-TERMINAL-SUPPRESSION** | **Gated Terminal Arrival Suppression & Short-Turn Safety (Station 3 / PE.13):** (1) Suppress terminating arrivals (`6 to Pelham Bay Park EXP`) at northern rail terminus `Pelham Bay Park` (`601`) from appearing as departures under `UPTOWN & BRONX`. (2) Protect mid-corridor short-turns (e.g. 6 train at Parkchester `611`) from false-positive suppression caused by rolling feed window cutoffs: enforce that `idx == count - 1 AND destination matches stopName` **only** suppresses departures at vetted terminal stops (`isTerminatingArrival` / static GTFS terminal set), defaulting to showing departures at intermediate stations. | **S** | ✅ Done |
| **Pre-T.4** | **WPT4-COMPLEX-MODAL-ISOLATION** | **Station Complex Modal Isolation & One-Way Curb Grounding (Station 2 / PE.4, PE.13):** (1) In `SpatialDatabaseManager.fetchStopDetails`, gate Tier 0 complex promotion by the tapped stop's `route_type` — bus stops (e.g. Kent Av & N 6 St `308667`) must never be promoted to heavy-rail subway complexes or assigned `modalClass = .subway`. (2) Pass `stopName` into `generateFallbackAvailableDirections` so one-way thoroughfares (Kent Av NB, Wythe Av SB) reliably detect single-direction curbs. (3) Preserve multi-modal hub separation at Atlantic Av–Barclays and Times Sq without mode collapse. | **S** | ✅ Done |
| **Pre-T.5** | **WPT5-INSPECTION-DOCK-HARDENING** | **Persistent Inspection Dock Synchronization & Non-Trapping Dismissal (Station 1 / FC-1, FC-7, PE.1, PE.2):** (1) In `MapView.swift`, assign `lastAppliedInspectionCommand = cmd` **unconditionally** whenever `command != nil`, ensuring live vehicle coordinate updates do not trigger stale camera framing during detent changes. (2) Replace `.interactiveDismissDisabled` with `onChange(of: selectedDetent)` snap-back clamping to `.inspectionPeekDetent` during inspection drag, eliminating user lock-in while preserving interactive swipe dismissal when not inspecting (FC-7). (3) Auto-scale destination text with `.lineLimit(1).minimumScaleFactor(0.85)` in `compactInspectionDockPill` and defensively strip `"\(line) to "` prefix. (4) Completely hide inspector header chrome during peek detent. | **S** | ✅ Done |
| **Pre-T.6** | **WPT6-LADDER-STEM-TRANSFERS** | **Continuous Ladder Stem, Express Variant Normalization & Stop-Level Route Gating (Station 5 / PE.11, WPD9):** (1) In `GuidewayRunInspector`, draw a continuous 4pt vertical stem through the entire 24x24 node background, eliminating 7pt transparent gaps above/below circle nodes. (2) Implement `trunkRouteId(for:)` normalizer to strip express variants (`6` on `6X`, `7` on `7X`, `F` on `FX`) and self-referential transfer badges (`route != lineInfo.line`). (3) Gate fallback departures by the individual stop's `routes` column rather than complex-level aggregated routes, preventing express routes (4/5) from leaking onto local-only stops (Astor Pl). | **S** | ⏳ Pending |
| **Pre-T.7** | **WPT7-FOG-DEGENERATE-RINGS** | **Fog Polygon Math Degenerate Ring Pruning (Station 6 / PE.8):** In `FogPolygonMath.swift`, prune degenerate disjoint rings (<3 vertices or area < 10m²) that produce stray floating amber polygon loops over water bodies (e.g., Goose Island / City Island). | **XS** | ⏳ Pending |

> [!IMPORTANT]
> **Pre-Wave T Verification & Exit Gate:**
> Prior to declaring Pre-Wave T complete and unblocking Wave T:
> 1. Implement TDD unit tests in `ComplexDirectionCanonicalizationTests.swift` and `TransitRevealSheetTests.swift` covering single-tracking direction overrides, short-turn non-suppression, and modal isolation.
> 2. Run `./scripts/verify-ux.sh` and ensure all 14 Commuter Ergonomic Invariants (FC-1 through FC-14) pass with zero violations.


### 🏛️ Wave Q: Multi-Modal Complex Clustering & 2D Floorplans (Epic D & Bug 6)

> [!NOTE]
> **Station Architecture & Cartography:** Implements Research Docs 15, 16, and 20 to resolve fragmented station dots, unify heavy-rail subway with commuter rail (Penn/Moynihan, Grand Central, Atlantic Ave), serve departures in sub-0.10ms, and render continuous cross-scale transitions from 0D bullets to 2D floorplans without GPU stalls.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **Q.1** | **WQ1-COMPLEX-SCHEMA** | **Station Complex Resolution Schema & Regional Hub Anchors (Doc 15 & 16):** (1) Add `complexes` and `stop_resolution` tables (`WITHOUT ROWID`) to `transit.sqlite`. (2) Ingest MTA `Stations.csv` / `Complexes.csv` and Regional Hub Anchors (Penn Station `600001`, Grand Central `600002`, Atlantic Ave `600003`). (3) Composite key mapping `(feed_id, stop_id)`. (4) Conflate OSM pedestrian concourse footprints with GTFS complex centroids within 400m. | **M** | ✅ Done |
| **Q.2** | **WQ2-SUB-MILLISECOND-ENGINE** | **Sub-0.10ms Clustered Complex Departure Engine (Doc 16):** (1) Materialize `realtime_departures` table clustered on `(complex_id, departure_time, feed_id, child_stop_id, trip_id)` declared `WITHOUT ROWID`. (2) Eliminate `USE TEMP B-TREE FOR ORDER BY` via leading-edge clustered index scan (40–70µs latency). (3) GRDB `ComplexDepartureService` with `PRAGMA mmap_size = 2147483648`, `PRAGMA locking_mode = EXCLUSIVE`, cached statements, and positional row parsing (`row[0]`). | **M** | ✅ Done |
| **Q.3** | **WQ3-MULTISCALE-TRANSITIONS** | **MapLibre Multi-Scale Station Transition (Bug 6 & Doc 20):** Continuous paint-property cross-dissolve across $z \in [15.0, 16.5]$ with zero Earcut re-triangulation. Station bullets contract & fade ($z \in [15.0, 16.0]$), 2D station footprints emerge ($z \in [15.2, 16.2]$), platform lines fade in ($z \in [15.5, 16.5]$), and exit portals emerge ($z \in [16.0, 16.5]$). Strict 7-layer Z-stack placing footprints/platforms below Fog and bullets/exits above Fog. | **M** | ✅ Done |
| **Q.4** | **WQ4-2D-FLOORPLAN-VIEWER** | **Discrete 2D Multi-Level Floorplan Viewer (Doc 15 & 20):** Implement runtime floor filtering using type-safe predicate `CAST(level, 'NSString')` on `MLNVectorStyleLayer` to prune inactive vertical storeys from Metal command encoding. Discrete Layer Portal Model supporting stairs/elevators spanning multiple levels (`levels = "-1;-2"`). Interactive floor stepper pill in `TransitRevealSheet`. | **S** | ✅ Done |

### ⚡ Wave R: Corridor Pulse & Kinematic Telemetry Engine (Epic C)

> [!NOTE]
> **Real-Time Kinematics & Regularity:** Implements Research Docs 17 and 19 to provide smooth, physically accurate vehicle tracking along tracks, Osuna-Newell excess wait analytics, TCQSM bunching detection, and GPU-accelerated MapLibre vehicle layers.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **R.1** | **WR1-KINEMATIC-INTERPOLATOR** | **Subsurface Subway Kinematic Interpolator Core (Doc 17):** Implement `Derivee::Transit::SubwayPositionInterpolator` in C++20: orthogonal polyline projection onto `shapes.txt` line strings in conformal coordinates, R160/R142 traction trapezoidal velocity solver ($a=1.15, d=1.25\text{ m/s}^2$), $C^2$-continuous Quintic Hermite smootherstep fallback for compressed schedules, mid-tunnel approach clamp at $\lambda = 0.85$ during signal holds, and stale feed freezing (>90s). | **M** | ✅ Done |
| **R.2** | **WR2-CORRIDOR-REGULARITY** | **Corridor Pulse Regularity Analytics (Doc 19):** Implement `AnalyzeCorridorRegularity` in Go Observer: single-pass Welford algorithm for mean $\mu_h$, standard deviation $\sigma_h$, and coefficient of variation $CV_h$. TCQSM bunching ($h_i < 0.25 H_{\text{sched}}$) and gap ($h_i > 1.75 H_{\text{sched}}$) detection. Composite score $S_{\text{corridor}} \in [0, 100]$ and tri-tier status (Good, Gaps, Delayed) with $<1.0\text{ms}$ latency budget. | **M** | ✅ Done |
| **R.3** | **WR3-CORRIDOR-MAP-LAYERS** | **MapLibre Real-Time Vehicle & Bunching Layers (Doc 19):** Implement `CorridorPulseMapController` in Swift: decoupled `MLNShapeSource` for transient vehicle markers and bunching segments. `MLNLineStyleLayer` casing + dashed line for platooning segments, `MLNCircleStyleLayer` for vehicle tokens with selection halos, and `MLNSymbolStyleLayer` with `transit-bearing-arrow` for heading tracking. Background Protobuf parsing keeping main thread at 60–120 FPS. | **M** | ✅ Done |

### 📱 Wave S: Out-of-Process Tracking Intent & System Extensions (Epic B)

> [!NOTE]
> **iOS 18 System Integration:** Exposes ambient CoreLocation tracking to iOS system surfaces (Control Center, Action Button, Lock Screen, Shortcuts) via App Intents executable out-of-process.

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **S.1** | **WS1-TRACKING-APP-INTENT** | **`ToggleAmbientTrackingIntent` & App Intent Suite:** Implement `ToggleAmbientTrackingIntent` conforming to `AppIntent` and `AudioRecordingIntent`. Direct Darwin notification or shared `UserDefaults(suiteName:)` IPC to command `AmbientTrackingEngine` without cold-launching the full MapLibre/GRDB hierarchy. | **S** | ✅ Done |
| **S.2** | **WS2-CONTROL-WIDGETS** | **Control Center & Lock Screen Widget Extension:** Implement iOS 18 `ControlWidgetToggle` for Control Center and Action Button. State synchronization reflecting active tracking status and hex exploration metrics in real time. | **S** | ✅ Done |

### 🔮 Wave O: MetalFogEngine & GPU Spatial Rendering Pipeline

> [!NOTE]
> **Wave O Architecture:** Upgrades the fog rendering pipeline from MapLibre CPU geometry generation (`cellsToLinkedMultiPolygon` shape assignment) to a direct, hardware-accelerated **Metal in-pipeline custom style layer (`MLNCustomStyleLayer`)**. Built strictly on Research Docs 05–09 in [`docs/research/`](file:///Volumes/T7ssd/derivee/docs/research/).
>
> **Core Constraints & Invariants:**
> - **Zero-Lag VSync:** EPSG:3857 $\rightarrow$ Metal NDC Relative-to-Center (RTC) projection with 0-frame latency (Doc 05).
> - **GPU Memory Cap:** Open-addressing GPU hash table in shared `MTLBuffer` strictly $< 10\text{ MB}$ VRAM (Doc 06).
> - **Compositing & Label Legibility:** In-pipeline injection below `MLNSymbolStyleLayer` preserving text legibility (Doc 07).
> - **Anti-Aliasing & Glow:** 4-vertex full-screen quad fragment shader with screen-space derivative AA (`dfdx`/`dfdy`) and single-pass Electric Amber (`#FFB300`) analytical glow (Doc 09).
> - **Performance Target:** Headless 120 FPS CADisplayLink telemetry, $\text{P99} \le 8.33\text{ms}$, 0% CPU geometry time, $\text{RMSD} < 0.001$ visual parity (Doc 08).

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **O.1** | **WO1-CAMERA-PROJECTION** | **Web Mercator to Metal NDC RTC Projection & VSync Engine (Doc 05):** Implement `MapProjectionMath` translating EPSG:3857 coordinates to Metal Normalized Device Coordinates (NDC) using Relative-to-Center (RTC) precision offsets ($S(z) = 512 \cdot 2^z$) mitigating float32 jitter at $z \ge 18$. Zero-lag CADisplayLink camera synchronization with MapLibre viewport. | **M** | ✅ Done |
| **O.2** | **WO2-GPU-SPATIAL-HASH** | **Open-Addressing GPU Spatial Hash Table in `MTLBuffer` (Doc 06):** 16-byte aligned `H3HashSlot` and 64-byte `H3HashTableHeader` in shared `MTLBuffer` (<4.26 MB VRAM for 100k hexes, <10 MB budget). In-shader bitwise parent LOD aggregation (Res 11 $\rightarrow$ Res 10/9/8). `.storageModeShared` zero-copy CPU pointer writes. | **L** | ✅ Done |
| **O.3** | **WO3-LAYER-INJECTION** | **In-Pipeline `MLNCustomStyleLayer` Metal Injection & Label Occlusion (Doc 07):** Direct `id<MTLRenderCommandEncoder>` reuse within MapLibre render pipeline. Z-index insertion below `MLNSymbolStyleLayer` preserving station/street label legibility with zero auxiliary texture overhead. | **M** | ✅ Done |
| **O.4** | **WO4-MSL-QUAD-SHADERS** | **MSL Full-Screen Quad Fragment Pipeline, Derivative AA & Analytical Amber Glow (Doc 09):** 4-vertex full-screen quad vertex shader ($O(1)$ vertex load). Screen-space partial derivatives (`dfdx`, `dfdy`) for constant 1.0–1.5px boundary anti-aliasing across $z0 \dots z18$. Single-pass analytical Hermite glow ($I_{\text{glow}}(C)$) in Electric Amber (`#FFB300`) executing in $<0.3\text{ms}$ on Apple Silicon. | **M** | ✅ Done |
| **O.5** | **WO5-120HZ-TELEMETRY** | **Empirical 120 FPS CADisplayLink Telemetry Suite & Visual Parity Tests (Doc 08):** Automated headless XCTest telemetry suite verifying locked $\ge 118\text{ FPS}$ (P99 $\le 8.33\text{ms}$), 0% CPU geometry time ($\le 0.05\text{ms}$ uniform write), and $\text{RMSD} < 0.001$ visual parity against MapLibre polygon reference. | **M** | ✅ Done |

### 🧭 Wave T: Navigation Hardening & End-to-End Trip Flow

> [!IMPORTANT]
> **Commuter Navigation Integrity & End-to-End Field Flow:** Hardens the on-device routing engine, comparison sheets, and turn-by-turn guidance into a seamless, production-grade commuter experience. Built strictly on Research Docs 10, 11, 14, 15, and 17.
>
> **Gated on:** All PE wave field bugs resolved, `verify-ux.sh` green (FC-1 through FC-14). Binary routing assets (`timetable.bin`) must be compiled and bundled as the first task — they do not yet exist in production packs.
>
> **Execution Cadence across 3 Structured Phases:**
> - **Phase 1 (End-to-End Core Flow):** Resolves binary routing pack ingestion (T.1), hardens the Search → Compare → Guidance seam (T.2), establishes the session lifecycle and Live Activity (T.3), implements Apple/Google Maps arrival confirmation with background GRDB logging (T.4), and gates further work on a physical NYC field test (T.5).
> - **Phase 2 (Active Guidance Fidelity):** Delivers subsurface kinematic dead-reckoning and platform signal resynchronization (T.6), subterranean car positioning and exit guidance (T.7), and compass/heading jitter stabilization (T.8).
> - **Phase 3 (Recovery, Invariants & Accessibility):** Implements calm transfer connection latching and disruption rerouting (T.9), multi-city boundary guards (T.10), and step-free accessibility filtering (T.11).

#### Phase 1: End-to-End Core Flow (Sequential)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **T.1** | **WT1-PACK-V2-INGESTION** | **NYC City Pack v2 Compilation & Bundle Ingestion:** (1) Run Go `pack_builder` with `--build-timetable`, `--build-ultra`, and `--walk-graph` to produce a full NYC v2 pack containing `timetable.bin`, `ultra_transfers.csr`, and `walk_graph.bin` alongside existing `transit.sqlite` and `transit-lines.geojson`. (2) Embed binary assets into `city-nyc.pack.zst` in the app bundle. (3) In `CityPackManager.unpackAndInstall`, extract and validate binary routing assets. (4) Verify zero-copy `mmap` loading in `JourneyPlanner.configureForCity(slug: "nyc")` sets `isReady == true` within the 30 MB Jetsam ceiling. | **M** | Planned |
| **T.2** | **WT2-FLOW-SEAM** | **Search → Compare → Guidance Integration Seam:** (1) In `ContentView.swift`, harden `ActiveSheet` transitions (`.search` → `.routeComparison` → `.navigation`), eliminating sheet presentation flashes and background view retention. (2) In `PlaceSearchView`, pass origin GPS and tapped destination to `startRouteComparison`. (3) In `RouteComparisonListView`, verify tapping "Start Journey" injects the full `JourneyItinerary` into `NavigationGuidanceSheet`. (4) Synchronize camera framing: full polyline bounding box during comparison; smooth zoom to first leg origin on navigation begin. (5) Seamless back navigation without state leaks. | **M** | Planned |
| **T.3** | **WT3-SESSION-LIFECYCLE** | **Active Navigation Session Manager Lifecycle & On-Demand GBFS:** (1) Establish `MultimodalTripNavigationManager` as the single source of truth for active trip state. (2) Implement leg auto-advance triggered by GPS proximity (walking leg arrival, station entry geofences). (3) Wire on-demand `GBFSSyncService` polling lifecycle: start polling on entering Screen 4B/4C with cycling legs; cancel polling immediately on session teardown or sheet dismissal. (4) Manage navigation-specific `Activity<MultimodalTripAttributes>` lifecycle, ensuring updates persist across app suspension via `CLBackgroundActivitySession`. (5) Clean session teardown on manual cancellation. | **M** | Planned |
| **T.4** | **WT4-ARRIVAL-UX-PERSISTENCE** | **Apple/Google Maps Arrival UX & Background History Persistence:** (1) Auto-arrival detection when commuter is within 50m of final destination for $>15\text{s}$. (2) Present dedicated "You Have Arrived" summary card displaying trip duration, actual vs predicted arrival time, and a clean "Done" action. (3) Haptic celebration ping (`UINotificationFeedbackGenerator(.success)`). (4) Transition Live Activity to an `"Arrived"` terminal state with a 30s auto-dismiss timer. (5) Silently record completed trip metadata into a lightweight `completed_trips` GRDB table in `SpatialDatabaseManager` (origin, destination, timestamps, duration, modes), deferring Screen 3 UI additions. | **S** | Planned |
| **T.5** | **WT5-NYC-FIELD-TEST** | **🧑‍💻 Untethered NYC Field Walk & Subway Ride Checkpoint:** (1) Build T.1–T.4 on physical iPhone. (2) Conduct an untethered physical field test in NYC (street walk → subway station entry → subway ride → exit walk → arrival). (3) Validate search responsiveness, route card rendering, sheet detent behavior, Lock Screen Live Activity persistence across station signal drops, and auto-arrival triggering. (4) Export and inspect `pipeline_debug.log` via the iOS Files app; resolve all discovered P0/P1 interaction defects before unblocking Phase 2. | **S** | Planned |

#### Phase 2: Active Guidance Fidelity (Parallelizable after T.5)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **T.6** | **WT6-KINEMATIC-GUIDANCE** | **Subway Segment Kinematic Tracking & Platform Signal Resync:** (1) Wire C++20 `SubwayPositionInterpolator` (Doc 17) to MapLibre vehicle puck along route polyline during transit legs. (2) Subterranean dead-reckoning: advance position kinematically during in-tunnel cellular signal loss; seamlessly resynchronize schedule deviations and vehicle positions upon pulling into station platforms where cellular DAS is restored. (3) Terminal dwell clamping ($\lambda \equiv 0.0$) and mid-tunnel approach clamp ($\lambda = 0.85$) during signal holds $>90\text{s}$. (4) Approaching transfer station haptic alert 1 stop prior to exit. | **M** | Planned |
| **T.7** | **WT7-CAR-EXIT-GUIDANCE** | **Subterranean Car Positioning & Exit Guidance (Screen 4C):** (1) In `SubterraneanEgressEngine`, compute optimal boarding position (Front / Middle / Rear) based on destination egress stairs and transfer corridors per design.md §12.4. (2) Surface deterministic station exit labels (e.g. `Exit 4B - NW Corner 42nd & Broadway`) from station complexes schema on next-maneuver cards. (3) Render transfer corridor wayfinding prompts (e.g., `Follow signs to L platform; board near rear`) during multi-line subterranean station transfers. | **M** | Planned |
| **T.8** | **WT8-WALKING-GPS-HARDENING** | **Walking Segment GPS Cone & Compass Hardening:** (1) Apply exponential moving average smoothing ($\alpha = 0.3$) and minimum speed gate ($>1.2\text{ m/s}$) to heading vectors to eliminate walking compass jitter. (2) Urban canyon multipath filter rejecting location fixes with horizontal accuracy $>25\text{m}$. (3) Emphasize decision points using `LandmarkWalkingGuidanceEngine` cues and natural walking street references. (4) Soft off-route detection triggering subtle re-route suggestions if deviated $>75\text{m}$ for $>30\text{s}$. | **S** | Planned |

#### Phase 3: Recovery, Invariants & Accessibility (Parallelizable after T.5)
| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **T.9** | **WT9-CALM-TRANSFER-RECOVERY** | **Dynamic Rerouting & Calm Transfer Recovery:** (1) In `MissedConnectionDetector`, enforce station geofence gating and departure grace buffers ($+120\text{s}$) to eliminate false-alarm missed connections while boarding. (2) Account for realistic physical walking durations through complex transfer passageways (e.g. Times Sq $\leftrightarrow$ PABT). (3) Calm connection latching: if a tight connection is missed, automatically update guidance to the follow-on train on the same line without alarmist popups or emergency reroute calculations. (4) Surface a persistent 1-tap override pill (`"I made this train"` / `"Missed train"`). (5) Inline GTFS-RT disruption banner and 1-tap detour planning. | **M** | Planned |
| **T.10** | **WT10-MULTI-CITY-BOUNDARIES** | **Multi-City Navigation Boundaries & Asset Scoping:** (1) Verify all navigation view models strictly resolve assets dynamically via `activeCitySlug` (zero hardcoded `"nyc"`). (2) Enforce bounding-box spatial guards: reject search destinations outside the active city's bounding box with a clear `"Destination outside active city"` banner. (3) Hook `prepareForCitySwap()` to cleanly terminate active navigation sessions and Live Activities if a city switch is triggered in Settings. (4) Keep Boston MBTA binary pack compilation deferred to a dedicated city pack wave. | **S** | Planned |
| **T.11** | **WT11-STEP-FREE-ROUTING** | **Accessibility & Step-Free Route Filtering:** (1) In C++ routing engine, support step-free Pareto queries filtering out edges lacking `WHEELCHAIR_ACCESSIBLE` flags. (2) Exclude walk graph stairways lacking elevator or ramp alternatives. (3) Badge route comparison cards with ♿ when the accessible profile is selected. (4) Degraded state handling: provide honest empty states when no step-free route exists between selected points. | **S** | Planned |

### Post-Ship (Completed Post-Launch Waves)

| Wave | Task ID | Title | Size | Status |
|:---:|:---:|:---|:---:|:---:|
| **11.7a** | **W11.7a-HEATMAP** | **24 × 7 Station Reliability Heatmap Matrix:** Replace `TransitSparklineView` with an immediate-mode SwiftUI `Canvas` rendering 168 cells ($24 \times 7$) in <1.2ms. Universal 6-tier Cividis CVD-safe colormap (`#00224E` → `#FDEA45`) mapping $OTP\%$. $51\text{pt} \times 44\text{pt}$ cell touch target math (Apple HIG). Tapping a cell opens Tier 2 inspector sheet displaying SF Mono readouts for $OTP\%$, Median Delay $\tilde{D}$, $P90$, Headway Variance $\sigma^2_{\Delta t}$, and $EWT$. **Gated on:** `stop_reliability_hourly` populated with $\ge 7$ days of data (or verified via deterministic test fixtures in `DeriveeTests`). | **M** | ✅ Done |
| **11.7b** | **W11.7b-DEPARTURES** | **Hour × Minute Departure Matrix & Timetable Inspector:** Add `[ Live Arrivals | Full Timetable ]` segmented control to `TransitRevealSheet` (Screen 2). Full 24-hour departure timetable with dynamic horizontal minute pills (`02, 08, 15, 22...`), line-colored branch/express badges, first/last departure boundary markers, real-time $\Delta t$ countdown overlays with pulsing dot, and alert delay badges (`+4m`). **Gated on:** `stop_events` actively populated + `W11.7a-HEATMAP` shipped. | **L** | ✅ Done |
| **11.7c** | **W11.7c-LIVE-PULSE** | **Live Transit Feed Radar Pulse & Instant Refresh Control:** (1) `LiveStatusBadge` with Electric Amber (`#FFB300`) breathing core dot + expanding radar ping (`1.0 → 1.8` scale) and graceful dimmed fallback to `SCHEDULED` when offline. (2) `CircularRefreshButton` with smooth 15s progress ring arc and $360^\circ$ spin animation on tap. (3) Generational polling loop cancellation preventing double-polling bursts on manual refresh. (4) Light haptic feedback (`UIImpactFeedbackGenerator`). (5) Unit and snapshot tests in `TransitRevealSheetTests`. | **S** | ✅ Done |
| — | W15-DYNAMIC-ISLAND | Dynamic Island Support for Stat Bar | S | ✅ Done |

---

## Testing Strategy & Rollout

Broadening the overall testing strategy across upcoming waves. We are adopting a **staggered approach** to ensure a solid foundation before building further UI:

### Phase 1–3: Complete ✅

All core engine, spatial math, transit hydration, snapshot, and live observation tests are implemented and passing across 9 test files (34 tests total, 0 skips). Test architecture uses cooperative polling (`while` loop + `RunLoop.current.run` + `Task.sleep`) for all `@Observable` assertions — legacy `XCTestExpectation` and `withObservationTracking` are banned.

All live `ValueObservation` → `recomputeFogShape()` reactivity tests (`SpatialStoreFogTests.swift` and `GRDBObservationTests.swift`) are actively verified and passing in CI.

### Phase 4: Local Shift-Left Ergonomics & Test Bouncer (Complete ✅)

Remote GitHub Actions CI (`.github/workflows/ci.yml`) has been retired and superseded by a 100% local, two-tier "Local Ergonomic Bouncer" engineered specifically for solo vibe-coding (see [Research Document 21: `docs/research/21_mobile_map_transit_testing_invariants.md`](file:///Volumes/T7ssd/derivee/docs/research/21_mobile_map_transit_testing_invariants.md)):

1. **Tier 1: Pre-Commit AST/Pattern Bouncer (`.githooks/pre-commit`):** Sub-100ms local git hook that intercepts commits on staged Swift code. Blocks hardcoded row heights (`.frame(height: 56)`), raw telemetry acronyms (`trip_id`, `AVL`, `GTFS-RT VP`), nested bottom sheet presentations (`.sheet`), and unanchored stop ladders before commits enter history.
2. **Tier 2: Headless Commuter Ergonomics Suite (`scripts/verify-ux.sh`):** Headless test runner executing [`CommuterErgonomicsTests.swift`](file:///Volumes/T7ssd/derivee/DeriveeNative/DeriveeTests/CommuterErgonomicsTests.swift) (44 assertions across FC-1 to FC-6, 0.0s Glance Budget, and Degraded State Handling) in under 18 seconds without flaky visual snapshot diffs. Includes a curated ASCII progress reporter and automatically self-configures `git config core.hooksPath .githooks`.

> [!TIP]
> **Solo Vibe Coding Workflow:** Pushes go directly to `main` without PR delays or remote CI queues. The local bouncer provides instant, reliable feedback directly on the developer's workstation prior to every commit and push. All UI changes must satisfy the **UI Wave Definition of Done** defined in [`AGENTS.md`](file:///Volumes/T7ssd/derivee/AGENTS.md).
